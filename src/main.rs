// AUTO-GENERATED by cmaes_search.py — DO NOT EDIT
use ahash::AHasher;
use dashmap::DashMap;
use ndarray::{azip, s, Array1, Array2, Axis};
use num_complex::Complex;
use rand::rngs::{SmallRng, StdRng};
use rand::seq::SliceRandom;
use rand::{Rng, SeedableRng};
use rand_distr::{Distribution, StandardNormal};
use rayon::prelude::*;
use std::hash::{Hash, Hasher};
use std::io::Write as _;
use std::sync::Arc;

// ─── アーキテクチャ ハイパーパラメータ ───────────────────────────────────────
const N_LAYERS: usize = 2;
const LAYER_LEN: [usize; N_LAYERS] = [6, 8];
const N_INPUTS_MAIN: usize = 2;
/// ADF の外部入力: [in0, in1, one, x_global]
/// x_global はトップ層の ext[0]（入力 x）を全 ADF 層に伝播させたもの。
const N_INPUTS_ADF: usize = 4;
const N_ADF_PER_LAYER: [usize; N_LAYERS - 1] = [2];

const ONE_SIG: Sig = 0xFFFF_FFFF_FFFF_FFFFu64;
/// x_global の固定 Sig。バッチ変化時は exec_adf に渡す x_sig 引数で上書きされる。
const X_GLOBAL_SIG_SLOT: usize = 3;
const VEC_LEN: usize = 693;
const POP_SIZE: usize = 1514;
const ELITE: usize = 26;
const N_GEN: usize = 99999999;  // 時間制限で止める
const PROB_EML: f64 = 0.07020386;
const A: f64 = -1.43631465;
const B: f64 = 0.12114622;
const C: f64 = -0.30004312;
const D: f64 = 0.27610193;
const E: f64 = 1.69324465;
const HILO: f64 = 10.47126613;
const P: f64 = 0.88501658;

// ─── 突然変異率パラメータ ────────────────────────────────────────────────────
const MUT_STOP_PROB: f64 = 6.05395501;
const MUT_MAX_TARGETS: usize = 5;

// ─── learned mutator / mixer hyperparams ─────────────────────────────────────
const LEARNED_MUTATION_PROB: f64 = 0.66867610;
const MIXER_D1: usize = 9;
const MIXER_D2: usize = 80;
const MIXER_BLOCKS: usize = 4;
const MIXER_TOKEN_HIDDEN: usize = 72;
const MIXER_CHANNEL_HIDDEN: usize = 275;
const MIXER_COND_PROJ_HIDDEN: usize = 50;
const MIXER_COND_VEC_DIM: usize = 128;
const MIXER_WARMUP_GENERATIONS: usize = 2;
const MIXER_TRAIN_EVERY: usize = 3;
const MIXER_TRAIN_EPOCHS: usize = 2;
const MIXER_BATCH_SIZE: usize = 26;
const MIXER_TRAIN_POP_SUBSET: usize = 30;
const MIXER_MAX_TEACHER_PAIRS: usize = 1243;
const MIXER_MAX_MINIBATCHES: usize = 7;
const MIXER_LR_MATRIX: f32 = 0.02949220;
const MIXER_LR_VECTOR: f32 = 0.00284866;
const NEAREST_BETTER_CACHE_SIZE: usize = 4889;
const MUON_WEIGHT_DECAY: f32 = 0.00247846;
const MUON_MOMENTUM: f32 = 0.96472211;
const MUON_NS_STEPS: usize = 12;
const MUON_NESTEROV: bool = true;
const ADAMW_BETA1: f32 = 0.93726507;
const ADAMW_BETA2: f32 = 0.99925860;
const ADAMW_EPS: f32 = 0.000000531864;
const ADAMW_WEIGHT_DECAY: f32 = 0.03194219;
const EVAL_PROGRESS_CHUNK: usize = 32;
const TRAIN_PROGRESS_EVERY: usize = 4;

// ─── カリキュラム学習 ────────────────────────────────────────────────────────
const CURRICULUM_RAMP_GENS: usize = 1;
const INTER_WEIGHT_MAX: f64 = 1.0;

// ─── adf_cache 上限 ──────────────────────────────────────────────────────────
const ADF_CACHE_MAX: usize = 1 << 18;

// ─── バッチ設定 ──────────────────────────────────────────────────────────────
const N_BATCHES: usize = 1;


// ─── stdout フラッシュマクロ ──────────────────────────────────────────────────
// パイプ経由で起動されると stdout はブロックバッファになるため、
// 進捗ログは毎回即時フラッシュして Python 側が正確なタイミングで受信できるようにする。
macro_rules! println_flush {
    ($($arg:tt)*) => {{
        println!($($arg)*);
        let _ = std::io::stdout().flush();
    }};
}

// ─── レイヤー設定ヘルパー ─────────────────────────────────────────────────────

#[inline]
fn is_top(layer_idx: usize) -> bool {
    layer_idx == N_LAYERS - 1
}
#[inline]
fn layer_len(layer_idx: usize) -> usize {
    LAYER_LEN[layer_idx]
}
#[inline]
fn layer_n_ext(layer_idx: usize) -> usize {
    if is_top(layer_idx) { N_INPUTS_MAIN } else { N_INPUTS_ADF }
}
#[inline]
fn layer_n_adf(layer_idx: usize) -> usize {
    N_ADF_PER_LAYER[layer_idx]
}
#[inline]
fn layer_n_funcs(layer_idx: usize) -> usize {
    if layer_idx == 0 { 1 } else { 1 + layer_n_adf(layer_idx - 1) }
}

fn selectfunc(max: u8, rng: &mut SmallRng) -> u8 {
    if rng.gen::<f64>() < PROB_EML { return 0; }
    rng.gen_range(0..max)
}

// ─── 複素数ユーティリティ ─────────────────────────────────────────────────────

#[inline(always)]
fn clamp(z: Complex<f64>) -> Complex<f64> {
    const M: f64 = 15.0;
    Complex::new(z.re.clamp(-M, M), z.im.clamp(-M, M))
}
#[inline(always)]
fn safe_log(z: Complex<f64>) -> Complex<f64> {
    const EPS: f64 = 1e-6;
    if z.norm_sqr() < EPS * EPS { Complex::new(EPS, 0.0).ln() } else { z.ln() }
}
#[inline(always)]
fn eml(a: Complex<f64>, b: Complex<f64>) -> Complex<f64> {
    clamp(a).exp() - safe_log(b)
}
fn eml_vec(x: &[Complex<f64>], y: &[Complex<f64>]) -> Vec<Complex<f64>> {
    x.iter().zip(y).map(|(&a, &b)| eml(a, b)).collect()
}

// ─── Chromosome ───────────────────────────────────────────────────────────────

#[derive(Clone)]
struct Chromosome {
    layer_idx: usize,
    conn: Box<[[u16; 2]]>,
    func: Box<[u8]>,
}

impl Chromosome {
    fn random(layer_idx: usize, rng: &mut SmallRng) -> Self {
        let n = layer_len(layer_idx);
        let n_ext = layer_n_ext(layer_idx);
        let n_f = layer_n_funcs(layer_idx) as u8;
        let conn = (0..n)
            .map(|i| { let m = (n_ext + i) as u16; [((1.0 - rng.gen::<f64>().powf(P)) * m as f64).floor() as u16, ((1.0 - rng.gen::<f64>().powf(P)) * m as f64).floor() as u16] })
            .collect::<Vec<_>>().into_boxed_slice();
        let func = (0..n).map(|_| selectfunc(n_f, rng)).collect::<Vec<_>>().into_boxed_slice();
        Self { layer_idx, conn, func }
    }

    fn active_and_sig(&self) -> (Vec<usize>, u64) {
        let n = layer_len(self.layer_idx);
        let n_ext = layer_n_ext(self.layer_idx);
        let total = n_ext + n;
        let mut active = vec![false; total];
        active[total - 1] = true;
        for i in (0..n).rev() {
            if !active[n_ext + i] { continue; }
            active[self.conn[i][0] as usize] = true;
            active[self.conn[i][1] as usize] = true;
        }
        let mut h = AHasher::default();
        (self.layer_idx as u8).hash(&mut h);
        let list: Vec<usize> = (0..total).filter(|&i| active[i])
            .inspect(|&abs| {
                if abs >= n_ext {
                    let i = abs - n_ext;
                    self.conn[i][0].hash(&mut h); self.conn[i][1].hash(&mut h);
                    self.func[i].hash(&mut h); abs.hash(&mut h);
                }
            }).collect();
        (list, h.finish())
    }

    fn mutate(&self, rng: &mut SmallRng) -> Self {
        // 修正（前回提案2）: Box → Vec → Box の往復をなくす。
        // 変異後の conn/func を直接 Vec で組み立て、最後に into_boxed_slice() を1回だけ呼ぶ。
        let n = layer_len(self.layer_idx);
        let n_ext = layer_n_ext(self.layer_idx);
        let n_f = layer_n_funcs(self.layer_idx) as u8;
        let mut conn: Vec<[u16; 2]> = self.conn.to_vec();
        let mut func: Vec<u8>       = self.func.to_vec();
        // MUT_STOP_PROB: 対数一様分布の最大値。大きいほど多くのノードを変異させる。
        let n_mut = (rng.gen::<f64>() * MUT_STOP_PROB).exp() as usize + 1;
        for _ in 0..n_mut {
            let i = rng.gen_range(0..n);
            let max = (n_ext + i) as u16;
            match rng.gen_range(0..3u8) {
                0 => conn[i][0] = ((1.0 - rng.gen::<f64>().powf(P)) * max as f64).floor() as u16,
                1 => conn[i][1] = ((1.0 - rng.gen::<f64>().powf(P)) * max as f64).floor() as u16,
                _ => func[i] = selectfunc(n_f, rng),
            }
            if rng.gen::<f64>() < 0.05 {
                conn[i][rng.gen_range(0..2)] = rng.gen_range(0..n_ext as u16);
            }
        }
        Self { layer_idx: self.layer_idx, conn: conn.into_boxed_slice(), func: func.into_boxed_slice() }
    }

    fn mix(&self, rng: &mut SmallRng, other: &Chromosome) -> Self {
        let (active2, _) = other.active_and_sig();
        let n_ext = layer_n_ext(self.layer_idx);
        let mut conn = self.conn.to_vec();
        let mut func = self.func.to_vec();
        let mut cands: Vec<usize> = active2.into_iter().filter(|&i| i >= n_ext).collect();
        if !cands.is_empty() {
            let n_copy = rng.gen_range(1..=cands.len());
            for i in 0..n_copy.min(cands.len()) {
                let j = rng.gen_range(i..cands.len());
                cands.swap(i, j);
                let local = cands[i] - n_ext;
                conn[local] = other.conn[local];
                func[local] = other.func[local];
            }
        }
        Self { layer_idx: self.layer_idx, conn: conn.into_boxed_slice(), func: func.into_boxed_slice() }
    }
}

// ─── Genome ───────────────────────────────────────────────────────────────────

#[derive(Clone)]
struct Genome { layers: Vec<Vec<Chromosome>> }

impl Genome {
    fn random(rng: &mut SmallRng) -> Self {
        let layers = (0..N_LAYERS).map(|li| {
            let count = if is_top(li) { 1 } else { layer_n_adf(li) };
            (0..count).map(|_| Chromosome::random(li, rng)).collect()
        }).collect();
        Self { layers }
    }

    fn mutate(&self, rng: &mut SmallRng) -> Self {
        let mut g = self.clone();
        // 修正F: totals / grand_total は N_LAYERS と N_ADF_PER_LAYER が
        // コンパイル時定数なので、OnceLock で初回のみ計算して使い回す。
        // POP_SIZE × N_GEN 回の Vec 確保・計算がゼロになる。
        use std::sync::OnceLock;
        static TOTALS_CACHE: OnceLock<(Vec<usize>, usize)> = OnceLock::new();
        let (totals, grand_total) = TOTALS_CACHE.get_or_init(|| {
            let t: Vec<usize> = (0..N_LAYERS)
                .map(|li| if is_top(li) { 1 } else { layer_n_adf(li) }).collect();
            let gt: usize = t.iter().sum();
            (t, gt)
        });
        // MUT_MAX_TARGETS: 一度に変異させる Chromosome の最大数
        let n_targets = rng.gen_range(1..=MUT_MAX_TARGETS.min(*grand_total));
        for _ in 0..n_targets {
            let mut t = rng.gen_range(0..*grand_total);
            for (li, &cnt) in totals.iter().enumerate() {
                if t < cnt { g.layers[li][t] = self.layers[li][t].mutate(rng); break; }
                t -= cnt;
            }
        }
        g
    }

    fn mix(&self, rng: &mut SmallRng, other: &Genome) -> Self {
        let mut g = self.clone();
        for li in 0..N_LAYERS {
            let cnt = if is_top(li) { 1 } else { layer_n_adf(li) };
            for k in 0..cnt {
                if rng.gen::<f64>() < 0.5 {
                    g.layers[li][k] = self.layers[li][k].mix(rng, &other.layers[li][k]);
                }
            }
        }
        g
    }
}

// ─── Signature ────────────────────────────────────────────────────────────────

type Sig = u64;

#[inline]
fn make_sig(a: Sig, b: Sig, func_id: u8) -> Sig {
    let mut x = a ^ ((func_id as u64).wrapping_mul(0x9e3779b97f4a7c15));
    x ^= b.wrapping_mul(0x6c62272e07bb0142);
    x ^= x >> 30; x = x.wrapping_mul(0xbf58476d1ce4e5b9);
    x ^= x >> 27; x = x.wrapping_mul(0x94d049bb133111eb);
    x ^ (x >> 31)
}

fn genome_key(all_sigs: &[Vec<Sig>]) -> u64 {
    let mut h = AHasher::default();
    for layer_sigs in all_sigs { for &s in layer_sigs { s.hash(&mut h); } }
    h.finish()
}

// ─── Cache ────────────────────────────────────────────────────────────────────

type ArcVec = Arc<[Complex<f64>]>;
/// ADF キャッシュのキー。
/// (batch_sig, c_sig, in0_sig, in1_sig, x_sig)
/// x_sig を加えることで、同じ ADF 構造でもバッチの x 値が異なれば別エントリになる。
type AdfKey = (Sig, Sig, Sig, Sig, Sig);
type Score = (f64, f64);

struct NodeBuf { sigs: Vec<Sig>, vals: Vec<ArcVec> }

/// 修正E: VEC_LEN 個の Complex<0,0> からなるダミー Arc をプロセス中に一度だけ生成する。
/// NodeBuf::new() が呼ばれるたびに Arc::from(vec![...]) でヒープ確保していたコストを排除。
fn dummy_arc() -> ArcVec {
    use std::sync::OnceLock;
    static DUMMY: OnceLock<ArcVec> = OnceLock::new();
    Arc::clone(DUMMY.get_or_init(|| Arc::from(
        vec![Complex::new(0.0f64, 0.0f64); VEC_LEN].into_boxed_slice()
    )))
}

impl NodeBuf {
    fn new(total: usize) -> Self {
        // 修正E: static OnceLock から clone するだけ（Vec アロケーションゼロ）
        let dummy = dummy_arc();
        Self { sigs: vec![0u64; total], vals: vec![dummy; total] }
    }
    #[inline] fn set(&mut self, idx: usize, sig: Sig, val: ArcVec) { self.sigs[idx] = sig; self.vals[idx] = val; }
    #[inline] fn sig(&self, idx: usize) -> Sig { self.sigs[idx] }
    #[inline] fn val(&self, idx: usize) -> &ArcVec { &self.vals[idx] }
}

#[inline]
fn node_get_or_compute(sig: Sig, v0: &[Complex<f64>], v1: &[Complex<f64>], ev: &Evaluator) -> ArcVec {
    // キーに世代番号を混ぜることで reset_generation() が O(1) で済む。
    // 古い世代のエントリはヒットせず、自然に上書きされる。
    let gen_sig = sig ^ ev.current_gen_sig().wrapping_mul(0x9e3779b97f4a7c15);
    if let Some(v) = ev.node_cache.get(&gen_sig) { return Arc::clone(&*v); }
    let out: ArcVec = Arc::from(eml_vec(v0, v1).into_boxed_slice());
    ev.node_cache.entry(gen_sig).or_insert_with(|| Arc::clone(&out));
    out
}

// ─── Dataset ──────────────────────────────────────────────────────────────────

fn target_fn(x: Complex<f64>) -> Complex<f64> { (x * x * x - x).sin() * (x * x).sin()  }

fn make_batch(rng: &mut SmallRng, x_range: (f64, f64))
    -> ([Vec<Complex<f64>>; N_INPUTS_MAIN], Vec<Complex<f64>>)
{
    let (lo, hi) = x_range;
    let xs: Vec<Complex<f64>> = (0..VEC_LEN)
        .map(|_| Complex::new(lo + rng.gen::<f64>() * (hi - lo), 0.0)).collect();
    let one = vec![Complex::new(1.0, 0.0); VEC_LEN];
    let ys = xs.iter().map(|&x| target_fn(x)).collect();
    ([xs, one], ys)
}

/// 修正1（前回提案）: target の事前ソート結果を保持する構造体。
/// score_and_acc_into 内で毎回行っていた target 側ソートを排除する。
struct PrecomputedTarget {
    /// 4特徴量分のソート済みインデックス (t_order)
    t_orders: [Vec<usize>; 4],
    /// 4特徴量分のランク配列（pred ソート順での target ランク）
    t_ranks: [Vec<i64>; 4],
}

impl PrecomputedTarget {
    fn new(target: &[Complex<f64>]) -> Self {
        let n = target.len();
        let extractors: [fn(Complex<f64>) -> f64; 4] =
            [|z| z.re, |z| z.im, |z| z.norm(), |z| z.arg()];
        let mut t_bufs  = [vec![0.0f64; n], vec![0.0f64; n], vec![0.0f64; n], vec![0.0f64; n]];
        let mut t_orders = [vec![0usize; n], vec![0usize; n], vec![0usize; n], vec![0usize; n]];
        let mut t_ranks  = [vec![0i64; n],  vec![0i64; n],  vec![0i64; n],  vec![0i64; n]];
        for (fi, ext) in extractors.iter().enumerate() {
            for k in 0..n { t_bufs[fi][k] = ext(target[k]); }
            for k in 0..n { t_orders[fi][k] = k; }
            t_orders[fi].sort_unstable_by(|&a, &b|
                t_bufs[fi][a].partial_cmp(&t_bufs[fi][b])
                    .unwrap_or(std::cmp::Ordering::Equal));
            for (r, &i) in t_orders[fi].iter().enumerate() { t_ranks[fi][i] = r as i64; }
        }
        Self { t_orders, t_ranks }
    }
}

struct Dataset {
    batches: Vec<([Vec<Complex<f64>>; N_INPUTS_MAIN], Vec<Complex<f64>>)>,
    batch_sig: Sig,
    /// 修正C: exec_top に渡す Arc を事前生成しておく（毎個体ごとのアロケーション排除）
    ext_arcs: Vec<Vec<ArcVec>>,
    /// 修正1（前回提案）: target の事前ソート済みデータ
    precomputed: Vec<PrecomputedTarget>,
}

impl Dataset {
    /// 固定データセット: 起動時に一度だけ生成し、以降は変更しない。
    /// batch_sig も固定値になるため、世代をまたいだキャッシュが正しく機能する。
    fn new_fixed(rng: &mut SmallRng) -> Self {
        let batches: Vec<_> = (0..N_BATCHES).map(|_| make_batch(rng, (-HILO, HILO))).collect();
        let batch_sig = Self::compute_sig(&batches);
        // 修正C: バッチごとの入力 Arc を事前生成
        let ext_arcs: Vec<Vec<ArcVec>> = batches.iter().map(|(inputs, _)| {
            inputs.iter().map(|v| Arc::from(v.as_slice())).collect()
        }).collect();
        // 修正1（前回提案）: target のソートを事前計算
        let precomputed: Vec<PrecomputedTarget> = batches.iter()
            .map(|(_, target)| PrecomputedTarget::new(target))
            .collect();
        Self { batches, batch_sig, ext_arcs, precomputed }
    }
    fn compute_sig(batches: &[([Vec<Complex<f64>>; N_INPUTS_MAIN], Vec<Complex<f64>>)]) -> Sig {
        let mut h = AHasher::default();
        for (inputs, _) in batches { for z in inputs[0].iter().take(8) { z.re.to_bits().hash(&mut h); } }
        h.finish()
    }
}

// ─── Evaluator ────────────────────────────────────────────────────────────────

struct Evaluator {
    node_cache: DashMap<Sig, ArcVec>,
    adf_cache: DashMap<AdfKey, (Sig, ArcVec)>,
    /// 固定データセット運用では fitness_cache はゲノムが同一なら永続的に有効。
    /// 世代をまたいで保持し、新規ゲノムのみ計算する。
    fitness_cache: DashMap<u64, Score>,
    score_cache: DashMap<(Sig, Sig), Score>,
    /// node_cache の論理クリアに使う世代カウンタ。
    /// clear() によるエントリ単位のDrop（数万回のArc参照カウント操作）を回避し、
    /// キー空間に世代番号を混ぜることで O(1) で「全無効化」する。
    generation: std::sync::atomic::AtomicU64,
}

impl Evaluator {
    fn new() -> Self {
        Self {
            node_cache: DashMap::with_capacity(1 << 17),
            adf_cache: DashMap::with_capacity(1 << 16),
            fitness_cache: DashMap::with_capacity(POP_SIZE * 8),
            score_cache: DashMap::with_capacity(1 << 17),
            generation: std::sync::atomic::AtomicU64::new(0),
        }
    }

    /// 固定データセット運用向けリセット。
    /// - node_cache: 世代カウンタをインクリメントして論理クリア（O(1)、Drop コストゼロ）。
    ///   エントリが過剰に蓄積した場合のみ実際にクリアする。
    /// - fitness_cache / adf_cache / score_cache:
    ///   データセットが変わらないため保持。同一ゲノムは再評価不要。
    ///   ただし adf_cache がメモリ上限を超えたら解放する。
    fn reset_generation(&self) {
        // node_cache は論理クリア: カウンタを上げるだけで O(1)。
        // 古い世代のエントリは gen_sig が一致しないためヒットしない。
        // エントリ数が一定を超えたら実際にクリアしてメモリを回収する。
        self.generation.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        if self.node_cache.len() > (1 << 18) {
            self.node_cache.clear();
        }
        if self.adf_cache.len() > ADF_CACHE_MAX {
            self.adf_cache.clear();
            self.score_cache.clear();
            // fitness_cache は adf_cache に依存しないので保持してよい
        }
    }

    #[inline]
    fn current_gen_sig(&self) -> u64 {
        self.generation.load(std::sync::atomic::Ordering::Relaxed)
    }

    #[allow(clippy::too_many_arguments)]
    fn get_node_score(&self, batch_sig: Sig, node_sig: Sig, val: &ArcVec, target: &[Complex<f64>],
        p_buf: &mut [f64], t_buf: &mut [f64], order_buf: &mut [usize], rank_buf: &mut [i64],
        p_rank_buf: &mut [i64], t_order_buf: &mut [usize],
        precomp: &PrecomputedTarget) -> Score
    {
        let key = (batch_sig, node_sig);
        if let Some(s) = self.score_cache.get(&key) { return *s; }
        let s = score_and_acc_into(val, target, p_buf, t_buf, order_buf, rank_buf, p_rank_buf, t_order_buf, precomp);
        self.score_cache.insert(key, s); s
    }
}

// ─── 評価指標 ─────────────────────────────────────────────────────────────────

fn pearson_raw(x: &[f64], y: &[f64]) -> f64 {
    let n = x.len() as f64;
    let mx = x.iter().sum::<f64>() / n;
    let my = y.iter().sum::<f64>() / n;
    let (mut num, mut dx, mut dy) = (0.0, 0.0, 0.0f64);
    for (&xi, &yi) in x.iter().zip(y) {
        let a = xi - mx; let b = yi - my;
        num += a * b; dx += a * a; dy += b * b;
    }
    if dx < 1e-12 || dy < 1e-12 { 0.0 } else { num / (dx.sqrt() * dy.sqrt()) }
}

#[inline]
fn chatterjee_from_ranks(sorted_idx: &[usize], ranks: &[i64]) -> f64 {
    // 修正G: windows(2) のイテレータチェーンを明示的ループに置換。
    // コンパイラの autovectorization（SIMD）が効きやすくなる。
    let mut s: i64 = 0;
    let n = sorted_idx.len();
    for i in 0..n.saturating_sub(1) {
        s += (ranks[sorted_idx[i]] - ranks[sorted_idx[i + 1]]).abs();
    }
    let nf = n as f64;
    1.0 - 3.0 * s as f64 / (nf * nf - 1.0)
}

/// 呼び出し元が 8 本のバッファを渡す。内部アロケーションゼロ。
/// p_rank_buf / t_order_buf は旧バージョンで毎回 vec! していたものを呼び出し元に移管。
/// 修正1（前回提案）: precomputed を受け取り、target 側ソートをスキップする。
fn score_and_acc_into(pred: &[Complex<f64>], target: &[Complex<f64>],
    p_buf: &mut [f64], t_buf: &mut [f64],
    order_buf: &mut [usize], rank_buf: &mut [i64],
    p_rank_buf: &mut [i64], t_order_buf: &mut [usize],
    precomp: &PrecomputedTarget,
) -> (f64, f64)
{
    let n = pred.len();
    let extractors: [fn(Complex<f64>) -> f64; 4] = [|z| z.re, |z| z.im, |z| z.norm(), |z| z.arg()];
    let mut total_score = 0.0f64;
    let mut max_pearson = 0.0f64;
    for (fi, ext) in extractors.iter().enumerate() {
        for k in 0..n { p_buf[k] = ext(pred[k]); t_buf[k] = ext(target[k]); }
        for k in 0..n { order_buf[k] = k; }
        order_buf[..n].sort_unstable_by(|&a, &b| p_buf[a].partial_cmp(&p_buf[b]).unwrap_or(std::cmp::Ordering::Equal));
        for (r, &i) in order_buf[..n].iter().enumerate() { p_rank_buf[i] = r as i64; }
        // 修正1: target 側ソートは事前計算済みのものを直接参照する（ソート・初期化コストゼロ）
        let t_order = &precomp.t_orders[fi];
        let rank_buf_pre = &precomp.t_ranks[fi];
        let c_fwd = chatterjee_from_ranks(&order_buf[..n], rank_buf_pre);
        let c_bwd = chatterjee_from_ranks(&t_order[..n], &p_rank_buf);
        let pe = pearson_raw(&p_buf[..n], &t_buf[..n]).abs();
        total_score += (1.0 - ((((c_fwd.max(0.0).powf(C) + c_bwd.max(0.0).powf(C)) / 2.0) * (1.0 - D) + pe.powf(C) * D)).powf(1.0 / C)).powf(E);
        if pe > max_pearson { max_pearson = pe; }
    }
    (total_score / 4.0, max_pearson)
}

// ─── 汎用 ADF 実行 ────────────────────────────────────────────────────────────

/// ADF を実行する。
///
/// `x_sig` / `x_arc` はトップ層の入力 x（ext[0]）をそのまま伝播させたもの。
/// ADF 内部のスロット 3 (`X_GLOBAL_SIG_SLOT`) に配置され、
/// ノードの conn がスロット 3 を指せば x_global を直接参照できる（カンニング）。
/// 再帰呼び出しでも同じ x_sig/x_arc を渡すことで全層で共有される。
fn exec_adf(
    layer_idx: usize, c: &Chromosome, c_sig: Sig, active: &[usize],
    in0_sig: Sig, in0: &ArcVec, in1_sig: Sig, in1: &ArcVec, one: &ArcVec,
    x_sig: Sig, x_arc: &ArcVec,
    genome: &Genome, all_sigs: &[Vec<Sig>], all_acts: &[Vec<Vec<usize>>],
    batch_sig: Sig, ev: &Evaluator,
) -> (Sig, ArcVec) {
    // x_sig をキーに含めることで、同一構造でも x が変われば別エントリになる
    let key: AdfKey = (batch_sig, c_sig, in0_sig, in1_sig, x_sig);
    if let Some(v) = ev.adf_cache.get(&key) { return (v.0, Arc::clone(&v.1)); }
    let n_ext = N_INPUTS_ADF; // = 4: [in0, in1, one, x_global]
    let total = n_ext + layer_len(layer_idx);
    let mut buf = NodeBuf::new(total);
    buf.set(0, in0_sig,  Arc::clone(in0));
    buf.set(1, in1_sig,  Arc::clone(in1));
    buf.set(2, ONE_SIG,  Arc::clone(one));
    buf.set(3, x_sig,    Arc::clone(x_arc)); // ← x_global スロット
    for &abs in active {
        if abs < n_ext { continue; }
        let i = abs - n_ext;
        let c0 = c.conn[i][0] as usize;
        let c1 = c.conn[i][1] as usize;
        let s0 = buf.sig(c0); let s1 = buf.sig(c1);
        let f = c.func[i] as usize;
        let (sig, val) = if f == 0 || layer_idx == 0 {
            let sig = make_sig(s0, s1, 0);
            let val = node_get_or_compute(sig, buf.val(c0), buf.val(c1), ev);
            (sig, val)
        } else {
            let sub_li = layer_idx - 1;
            let sub_idx = (f - 1).min(layer_n_adf(sub_li) - 1);
            exec_adf(
                sub_li, &genome.layers[sub_li][sub_idx], all_sigs[sub_li][sub_idx],
                &all_acts[sub_li][sub_idx], s0, buf.val(c0), s1, buf.val(c1), one,
                x_sig, x_arc, // ← 再帰先にも x を伝播
                genome, all_sigs, all_acts, batch_sig, ev,
            )
        };
        buf.set(abs, sig, val);
    }
    let result = (buf.sig(total - 1), Arc::clone(buf.val(total - 1)));
    ev.adf_cache.entry(key).or_insert_with(|| result.clone());
    result
}

// ─── 最終出力層 実行 ──────────────────────────────────────────────────────────

fn exec_top(
    genome: &Genome, top_active: &[usize], all_sigs: &[Vec<Sig>], all_acts: &[Vec<Vec<usize>>],
    ext: &[Vec<Complex<f64>>; N_INPUTS_MAIN], target: &[Complex<f64>],
    batch_sig: Sig, ev: &Evaluator,
    p_buf: &mut [f64], t_buf: &mut [f64], order_buf: &mut [usize], rank_buf: &mut [i64],
    p_rank_buf: &mut [i64], t_order_buf: &mut [usize],
    // 修正C: 事前生成済み Arc を受け取る（毎呼び出しのヒープアロケーション排除）
    ext_arcs: &[ArcVec],
    // 修正1: target の事前ソート結果を受け取り、ノードスコア計算時のソートをスキップ
    precomp: &PrecomputedTarget,
) -> (ArcVec, Sig, f64, usize) {
    let top_li = N_LAYERS - 1;
    let c = &genome.layers[top_li][0];
    let n_ext = N_INPUTS_MAIN;
    let total = n_ext + layer_len(top_li);
    let mut buf = NodeBuf::new(total);
    // 修正C: Arc::from(slice) の代わりに事前生成済み Arc を clone するだけ（アロケーションゼロ）
    for i in 0..n_ext { buf.set(i, i as Sig + 1, Arc::clone(&ext_arcs[i])); }
    let one_arc: ArcVec = Arc::clone(&ext_arcs[1]);
    let x_arc:   ArcVec = Arc::clone(&ext_arcs[0]);
    let x_sig: Sig = 1;
    let mut sum_score = 0.0; let mut count = 0;
    for &abs in top_active {
        if abs < n_ext { continue; }
        let i = abs - n_ext;
        let c0 = c.conn[i][0] as usize; let c1 = c.conn[i][1] as usize;
        let s0 = buf.sig(c0); let s1 = buf.sig(c1);
        let f = c.func[i] as usize;
        let (sig, val) = if f == 0 || top_li == 0 {
            let sig = make_sig(s0, s1, 0);
            let val = node_get_or_compute(sig, buf.val(c0), buf.val(c1), ev);
            (sig, val)
        } else {
            let sub_li = top_li - 1;
            let sub_idx = (f - 1).min(layer_n_adf(sub_li) - 1);
            exec_adf(
                sub_li, &genome.layers[sub_li][sub_idx], all_sigs[sub_li][sub_idx],
                &all_acts[sub_li][sub_idx], s0, buf.val(c0), s1, buf.val(c1), &one_arc,
                x_sig, &x_arc,
                genome, all_sigs, all_acts, batch_sig, ev,
            )
        };
        // 修正1: precomp を渡して get_node_score 内の target ソートをスキップ
        let (s, _) = ev.get_node_score(batch_sig, sig, &val, target,
            p_buf, t_buf, order_buf, rank_buf, p_rank_buf, t_order_buf, precomp);
        sum_score += s; count += 1;
        buf.set(abs, sig, val);
    }
    let out_sig = buf.sig(total - 1);
    (Arc::clone(buf.val(total - 1)), out_sig, sum_score, count)
}

// ─── Fitness 評価 ─────────────────────────────────────────────────────────────

impl Chromosome {
    /// シグネチャのみを返す軽量版。active ノードリストは計算しない。
    /// fitness_cache の早期ヒット確認に使用し、キャッシュミス時のみ
    /// active_and_sig() でフル計算を行う。
    fn sig_only(&self) -> Sig {
        self.active_and_sig().1
    }
}

fn eval(g: &Genome, ds: &Dataset, ev: &Evaluator, inter_weight: f64) -> (f64, f64) {
    let top_li = N_LAYERS - 1;

    // ── 修正A: fitness_cache の早期リターンを active_and_sig() より前に行う ──
    // active_and_sig() はグラフ逆走査＋Ahash を全 Chromosome について実行するため重い。
    // sig_only() で Sig だけを先に取り出し、キャッシュヒット時はそこで返す。
    let sigs_only: Vec<Vec<Sig>> = (0..N_LAYERS)
        .map(|li| g.layers[li].iter().map(|c| c.sig_only()).collect())
        .collect();
    let genome_base = genome_key(&sigs_only);
    let gkey = make_sig(make_sig(genome_base, ds.batch_sig, 0),
                        inter_weight.to_bits(), 1);
    if let Some(v) = ev.fitness_cache.get(&gkey) { return *v; }

    // キャッシュミス時のみ active ノードリストを計算する
    let all_data: Vec<Vec<(Vec<usize>, Sig)>> = (0..N_LAYERS)
        .map(|li| g.layers[li].iter().map(|c| c.active_and_sig()).collect()).collect();
    let all_sigs: Vec<Vec<Sig>> = all_data.iter()
        .map(|layer| layer.iter().map(|d| d.1).collect()).collect();
    let all_acts: Vec<Vec<Vec<usize>>> = all_data.into_iter()
        .map(|layer| layer.into_iter().map(|d| d.0).collect()).collect();
    let top_active = &all_acts[top_li][0];

    // 全バッファをここで一度確保し、バッチループ内で再利用する (アロケーションゼロ化)
    let mut p_buf       = vec![0.0f64;  VEC_LEN];
    let mut t_buf       = vec![0.0f64;  VEC_LEN];
    let mut order_buf   = vec![0usize;  VEC_LEN];
    let mut rank_buf    = vec![0i64;    VEC_LEN];
    let mut p_rank_buf  = vec![0i64;    VEC_LEN];
    let mut t_order_buf = vec![0usize;  VEC_LEN];
    let mut total_loss = 0.0; let mut total_acc = 0.0;
    for (batch_idx, (inputs, target)) in ds.batches.iter().enumerate() {
        let precomp = &ds.precomputed[batch_idx];
        let (pred, out_sig, sum_score, count) = exec_top(
            g, top_active, &all_sigs, &all_acts, inputs, target, ds.batch_sig, ev,
            &mut p_buf, &mut t_buf, &mut order_buf, &mut rank_buf,
            &mut p_rank_buf, &mut t_order_buf,
            // 修正C: Dataset に事前格納した Arc を渡す（毎回のアロケーション排除）
            &ds.ext_arcs[batch_idx],
            // 修正1（前回提案）: target の事前ソート結果を渡す
            precomp);
        let (final_s, final_a) = ev.get_node_score(ds.batch_sig, out_sig, &pred, target,
            &mut p_buf, &mut t_buf, &mut order_buf, &mut rank_buf,
            &mut p_rank_buf, &mut t_order_buf, precomp);
        let avg_inter = if count > 0 { (sum_score / count as f64).powf(1.0 / E) } else { 0.0 };
        let combined = avg_inter;
        total_loss += ((combined).powf(A) * B + (1.0 - final_a).powf(A) * (1.0 - B)).powf(1.0 / A);
        total_acc += final_a;
    }
    let n = ds.batches.len() as f64;
    let res = (total_loss / n, total_acc / n);
    ev.fitness_cache.insert(gkey, res); res
}

// ============================================================
// learned mutation model (from main(3).rs)
// ============================================================

// ============================================================
// math helpers
// ============================================================

fn gelu_scalar(x: f32) -> f32 {
    0.5 * x * (1.0 + (0.7978845608 * (x + 0.044715 * x.powi(3))).tanh())
}

fn gelu_derivative_scalar(x: f32) -> f32 {
    let u = 0.7978845608 * (x + 0.044715 * x.powi(3));
    let t = u.tanh();
    let sech2 = 1.0 - t * t;
    0.5 * (1.0 + t) + 0.5 * x * sech2 * 0.7978845608 * (1.0 + 3.0 * 0.044715 * x * x)
}

fn sigmoid_scalar(x: f32) -> f32 {
    if x >= 0.0 {
        let z = (-x).exp();
        1.0 / (1.0 + z)
    } else {
        let z = x.exp();
        z / (1.0 + z)
    }
}

fn fro_norm(x: &Array2<f32>) -> f32 {
    // map + sum より fold の方が一時イテレータを生成しない
    x.iter().fold(0.0f32, |acc, &v| acc + v * v).sqrt()
}

// ============================================================
// optimizer helpers: Muon for 2D, AdamW for 1D
// ============================================================

#[derive(Clone, Copy)]
struct OptimHyper {
    // Muon for 2D matrix parameters
    lr_matrix: f32,
    muon_weight_decay: f32,
    muon_momentum: f32,
    muon_ns_steps: usize,
    muon_nesterov: bool,

    // AdamW for 1D vector parameters
    lr_vector: f32,
    adamw_beta1: f32,
    adamw_beta2: f32,
    adamw_eps: f32,
    adamw_weight_decay: f32,
}

fn zeropower_via_newtonschulz5(g: &Array2<f32>, steps: usize) -> Array2<f32> {
    let (orig_r, orig_c) = g.dim();
    // 修正（前回提案4）: orig_r > orig_c の場合、計算は転置形状で走り出力時に再転置していた。
    // 転置 view を直接渡し標準形 (r<=c) に揃えることで、to_owned() が1回で済む。
    // 旧: g.t().to_owned() → 計算 → x.t().to_owned()  （アロケーション2回）
    // 新: g.t().to_owned() → 計算 → そのまま返す       （アロケーション1回）
    let transposed = orig_r > orig_c;
    let mut x = if transposed {
        g.t().to_owned()  // shape: (orig_c, orig_r) — 正方形に近い形
    } else {
        g.clone()
    };

    let norm = fro_norm(&x);
    if norm > 0.0 {
        x.mapv_inplace(|v| v / (norm + 1e-7));
    }

    let a = 3.4445_f32;
    let b = -4.7750_f32;
    let c = 2.0315_f32;

    for _ in 0..steps {
        let xx_t = x.dot(&x.t());
        // bmat = b*xx_t + c*xx_t^2 を中間配列1本で計算 (旧: 2本の mapv + 加算)
        let xx_t2 = xx_t.dot(&xx_t);
        let mut bmat = xx_t;
        azip!((bm in bmat.view_mut(), &xx2 in &xx_t2) *bm = b * *bm + c * xx2);
        // x = a*x + bmat*x を in-place で合成
        let bx = bmat.dot(&x);
        azip!((xi in x.view_mut(), &bxi in &bx) *xi = a * *xi + bxi);
    }

    // scale は行列の縦横比に基づく正規化係数
    let scale = (1.0_f32).max(orig_r as f32 / orig_c as f32).sqrt();
    // 修正（前回提案4）: transposed の場合は x がすでに (orig_c, orig_r) 形状なので
    // 再転置して (orig_r, orig_c) に戻す。to_owned() は1回のみ。
    let mut out = if transposed { x.t().to_owned() } else { x };
    out.mapv_inplace(|v| v * scale);
    out
}

fn muon_update_2d(
    grad: &Array2<f32>,
    momentum_buf: &mut Array2<f32>,
    beta: f32,
    ns_steps: usize,
    nesterov: bool,
) -> Array2<f32> {
    // in-place: momentum_buf = beta * momentum_buf + (1-beta) * grad
    // 中間配列ゼロ
    let one_minus_beta = 1.0 - beta;
    azip!((m in momentum_buf.view_mut(), &g in grad) *m = beta * *m + one_minus_beta * g);

    let raw = if nesterov {
        // (1-beta)*grad + beta*momentum — 中間配列1本、アロケーション最小
        let mut r = Array2::<f32>::zeros(grad.raw_dim());
        azip!((ri in r.view_mut(), &g in grad, &m in momentum_buf.view())
            *ri = one_minus_beta * g + beta * m
        );
        r
    } else {
        momentum_buf.clone()
    };

    zeropower_via_newtonschulz5(&raw, ns_steps)
}

fn adamw_update_1d(
    param: &mut Array1<f32>,
    grad: &Array1<f32>,
    m: &mut Array1<f32>,
    v: &mut Array1<f32>,
    step: &mut usize,
    hp: OptimHyper,
) {
    *step += 1;
    let t = *step as i32;
    let b1 = hp.adamw_beta1;
    let b2 = hp.adamw_beta2;
    let bc1 = 1.0 - b1.powi(t);
    let bc2 = 1.0 - b2.powi(t);

    // weight decay (in-place)
    let wd = 1.0 - hp.lr_vector * hp.adamw_weight_decay;
    param.mapv_inplace(|x| x * wd);

    // m, v 更新 (in-place, 中間配列ゼロ)
    azip!((mi in m.view_mut(), &gi in grad) *mi = b1 * *mi + (1.0 - b1) * gi);
    azip!((vi in v.view_mut(), &gi in grad) *vi = b2 * *vi + (1.0 - b2) * gi * gi);

    // param 更新 (in-place, m_hat / v_hat は変数に持たず直接計算)
    let lr = hp.lr_vector;
    let eps = hp.adamw_eps;
    azip!((pi in param.view_mut(), &mi in m.view(), &vi in v.view()) {
        let m_hat = mi / bc1;
        let v_hat = vi / bc2;
        *pi -= lr * m_hat / (v_hat.sqrt() + eps);
    });
}

// ============================================================
// trainable parameter wrappers
// ============================================================

#[derive(Clone)]
struct MatrixParam {
    w: Array2<f32>,
    gw: Array2<f32>,
    mom: Array2<f32>,
}

impl MatrixParam {
    fn new(rng: &mut StdRng, rows: usize, cols: usize, scale: f32) -> Self {
        let mut w = Array2::<f32>::zeros((rows, cols));
        for i in 0..rows {
            for j in 0..cols {
                let z: f32 = StandardNormal.sample(rng);
                w[[i, j]] = z * scale;
            }
        }
        Self {
            w,
            gw: Array2::zeros((rows, cols)),
            mom: Array2::zeros((rows, cols)),
        }
    }

    fn zeros(rows: usize, cols: usize) -> Self {
        Self {
            w: Array2::zeros((rows, cols)),
            gw: Array2::zeros((rows, cols)),
            mom: Array2::zeros((rows, cols)),
        }
    }

    fn zero_grad(&mut self) {
        self.gw.fill(0.0);
    }

    fn scale_grad(&mut self, s: f32) {
        self.gw.mapv_inplace(|v| v * s);
    }

    fn step_muon(&mut self, hp: OptimHyper) {
        let upd = muon_update_2d(
            &self.gw,
            &mut self.mom,
            hp.muon_momentum,
            hp.muon_ns_steps,
            hp.muon_nesterov,
        );
        self.w
            .mapv_inplace(|v| v * (1.0 - hp.lr_matrix * hp.muon_weight_decay));
        self.w -= &upd.mapv(|v| hp.lr_matrix * v);
    }
}

#[derive(Clone)]
struct VectorParam {
    w: Array1<f32>,
    gw: Array1<f32>,
    m: Array1<f32>,
    v: Array1<f32>,
    step: usize,
}

impl VectorParam {
    fn new(size: usize) -> Self {
        Self {
            w: Array1::zeros(size),
            gw: Array1::zeros(size),
            m: Array1::zeros(size),
            v: Array1::zeros(size),
            step: 0,
        }
    }

    fn zero_grad(&mut self) {
        self.gw.fill(0.0);
    }

    fn scale_grad(&mut self, s: f32) {
        self.gw.mapv_inplace(|v| v * s);
    }

    fn step_adamw(&mut self, hp: OptimHyper) {
        adamw_update_1d(
            &mut self.w,
            &self.gw,
            &mut self.m,
            &mut self.v,
            &mut self.step,
            hp,
        );
    }
}

// ============================================================
// Linear layer
// ============================================================

#[derive(Clone)]
struct Linear {
    w: MatrixParam, // [in_dim, out_dim]
    b: VectorParam, // [out_dim]
}

#[derive(Clone)]
struct LinearCache {
    x: Array2<f32>, // [batch, in_dim]
}

impl Linear {
    fn new(rng: &mut StdRng, in_dim: usize, out_dim: usize, scale: f32) -> Self {
        Self {
            w: MatrixParam::new(rng, in_dim, out_dim, scale),
            b: VectorParam::new(out_dim),
        }
    }

    fn zero_grad(&mut self) {
        self.w.zero_grad();
        self.b.zero_grad();
    }

    fn scale_grad(&mut self, s: f32) {
        self.w.scale_grad(s);
        self.b.scale_grad(s);
    }

    fn step(&mut self, hp: OptimHyper) {
        self.w.step_muon(hp);
        self.b.step_adamw(hp);
    }

    fn forward(&self, x: &Array2<f32>) -> (Array2<f32>, LinearCache) {
        let y = x.dot(&self.w.w) + &self.b.w;
        (y, LinearCache { x: x.clone() })
    }

    fn backward(&mut self, cache: &LinearCache, dy: &Array2<f32>) -> Array2<f32> {
        self.w.gw += &cache.x.t().dot(dy);
        self.b.gw += &dy.sum_axis(Axis(0));
        dy.dot(&self.w.w.t())
    }
}

// ============================================================
// Bilinear map: Y = L X R^T
// ============================================================

#[derive(Clone)]
struct BilinearMap {
    l: MatrixParam, // [out_r, in_r]
    r: MatrixParam, // [out_c, in_c]
}

#[derive(Clone)]
struct BilinearCache {
    x: Array2<f32>,  // [in_r, in_c]
    xr: Array2<f32>, // [in_r, out_c]
}

impl BilinearMap {
    fn new(
        rng: &mut StdRng,
        in_r: usize,
        in_c: usize,
        out_r: usize,
        out_c: usize,
        scale: f32,
    ) -> Self {
        Self {
            l: MatrixParam::new(rng, out_r, in_r, scale),
            r: MatrixParam::new(rng, out_c, in_c, scale),
        }
    }

    fn zero_grad(&mut self) {
        self.l.zero_grad();
        self.r.zero_grad();
    }

    fn scale_grad(&mut self, s: f32) {
        self.l.scale_grad(s);
        self.r.scale_grad(s);
    }

    fn step(&mut self, hp: OptimHyper) {
        self.l.step_muon(hp);
        self.r.step_muon(hp);
    }

    fn forward(&self, x: &Array2<f32>) -> (Array2<f32>, BilinearCache) {
        let xr = x.dot(&self.r.w.t());
        let y = self.l.w.dot(&xr);
        (y, BilinearCache { x: x.clone(), xr })
    }

    fn backward(&mut self, cache: &BilinearCache, dy: &Array2<f32>) -> Array2<f32> {
        self.l.gw += &dy.dot(&cache.xr.t());
        let dxr = self.l.w.t().dot(dy);
        self.r.gw += &dxr.t().dot(&cache.x);
        dxr.dot(&self.r.w)
    }
}

// ============================================================
// condition embedding
// ============================================================

#[derive(Clone)]
enum CondEncodingKind {
    RawVector,
    SinCosScalar,
}

#[derive(Clone)]
struct CondEmbedding {
    kind: CondEncodingKind,
    dim: usize,
    max_period: f32,
}

#[derive(Clone)]
struct CondEmbeddingCache {
    cond: Array1<f32>,
    emb: Array1<f32>,
}

impl CondEmbedding {
    fn new_raw_vec(dim: usize) -> Self {
        assert!(dim >= 1);
        Self {
            kind: CondEncodingKind::RawVector,
            dim,
            max_period: 10000.0,
        }
    }

    fn new_sincos(dim: usize, max_period: f32) -> Self {
        assert!(dim >= 2);
        Self {
            kind: CondEncodingKind::SinCosScalar,
            dim,
            max_period,
        }
    }

    fn output_dim(&self) -> usize {
        self.dim
    }

    fn forward(&self, cond: &Array1<f32>) -> (Array1<f32>, CondEmbeddingCache) {
        let emb = match self.kind {
            CondEncodingKind::RawVector => {
                assert_eq!(cond.len(), self.dim);
                cond.clone()
            }
            CondEncodingKind::SinCosScalar => {
                assert!(
                    !cond.is_empty(),
                    "SinCosScalar condition embedding expects at least one scalar input"
                );
                let scalar = cond[0];
                let half = self.dim / 2;
                let mut out = Array1::<f32>::zeros(self.dim);

                for i in 0..half {
                    let frac = i as f32 / half as f32;
                    let freq = self.max_period.powf(-frac);
                    let x = scalar * freq;
                    out[i] = x.sin();
                    out[half + i] = x.cos();
                }

                if self.dim % 2 == 1 {
                    out[self.dim - 1] = 0.0;
                }
                out
            }
        };

        (emb.clone(), CondEmbeddingCache { cond: cond.clone(), emb })
    }

    fn backward(&self, cache: &CondEmbeddingCache, demb: &Array1<f32>) -> Array1<f32> {
        match self.kind {
            CondEncodingKind::RawVector => {
                assert_eq!(demb.len(), cache.cond.len());
                demb.clone()
            }
            CondEncodingKind::SinCosScalar => {
                let mut dcond = Array1::<f32>::zeros(cache.cond.len());
                let scalar = cache.cond[0];
                let half = self.dim / 2;
                let mut grad0 = 0.0;
                for i in 0..half {
                    let frac = i as f32 / half as f32;
                    let freq = self.max_period.powf(-frac);
                    let x = scalar * freq;
                    grad0 += demb[i] * x.cos() * freq;
                    grad0 += demb[half + i] * (-x.sin()) * freq;
                }
                dcond[0] = grad0;
                dcond
            }
        }
    }
}

// ============================================================
// per-block condition projection
// cond_emb -> hidden -> cond_vec
// ============================================================

#[derive(Clone)]
struct CondProjector {
    fc1: Linear,
    fc2: Linear,
}

#[derive(Clone)]
struct CondProjectorCache {
    z1: Array2<f32>, // [1, hidden]
    fc1_cache: LinearCache,
    fc2_cache: LinearCache,
}

impl CondProjector {
    fn new(rng: &mut StdRng, in_dim: usize, hidden_dim: usize, out_dim: usize, scale: f32) -> Self {
        Self {
            fc1: Linear::new(rng, in_dim, hidden_dim, scale),
            fc2: Linear::new(rng, hidden_dim, out_dim, scale),
        }
    }

    fn zero_grad(&mut self) {
        self.fc1.zero_grad();
        self.fc2.zero_grad();
    }

    fn scale_grad(&mut self, s: f32) {
        self.fc1.scale_grad(s);
        self.fc2.scale_grad(s);
    }

    fn step(&mut self, hp: OptimHyper) {
        self.fc1.step(hp);
        self.fc2.step(hp);
    }

    fn forward(&self, emb: &Array1<f32>) -> (Array1<f32>, CondProjectorCache) {
        let x = emb.clone().insert_axis(Axis(0)); // [1, in_dim]
        let (z1, fc1_cache) = self.fc1.forward(&x);
        let a1 = z1.mapv(gelu_scalar);
        let (y, fc2_cache) = self.fc2.forward(&a1);
        (
            y.row(0).to_owned(),
            CondProjectorCache {
                z1,
                fc1_cache,
                fc2_cache,
            },
        )
    }

    fn backward(&mut self, cache: &CondProjectorCache, dy: &Array1<f32>) -> Array1<f32> {
        let dy2 = dy.clone().insert_axis(Axis(0)); // [1, out_dim]
        let da1 = self.fc2.backward(&cache.fc2_cache, &dy2);

        // 修正B: indexed_iter_mut + 2次元インデックス を
        // フラットイテレータに置換 → 境界チェック排除 + SIMD autovectorization 促進
        let mut dz1 = da1.clone();
        for (v, &z) in dz1.iter_mut().zip(cache.z1.iter()) {
            *v *= gelu_derivative_scalar(z);
        }

        let dx = self.fc1.backward(&cache.fc1_cache, &dz1);
        dx.row(0).to_owned()
    }
}

// ============================================================
// AdaLN with vector condition
// y = LN(x) * (1 + gamma(cond_vec)) + beta(cond_vec)
// ============================================================

#[derive(Clone)]
struct AdaLn {
    eps: f32,
    cond_dim: usize,

    gamma_w: MatrixParam, // [channels, cond_dim]
    gamma_b: VectorParam, // [channels]
    beta_w: MatrixParam,  // [channels, cond_dim]
    beta_b: VectorParam,  // [channels]
}

#[derive(Clone)]
struct AdaLnCache {
    xhat: Array2<f32>,
    inv_std: Array1<f32>,
    gamma: Array1<f32>,
    cond_vec: Array1<f32>,
}

impl AdaLn {
    fn new(channels: usize, cond_dim: usize) -> Self {
        Self {
            eps: 1e-5,
            cond_dim,
            gamma_w: MatrixParam::zeros(channels, cond_dim),
            gamma_b: VectorParam::new(channels),
            beta_w: MatrixParam::zeros(channels, cond_dim),
            beta_b: VectorParam::new(channels),
        }
    }

    fn zero_grad(&mut self) {
        self.gamma_w.zero_grad();
        self.gamma_b.zero_grad();
        self.beta_w.zero_grad();
        self.beta_b.zero_grad();
    }

    fn scale_grad(&mut self, s: f32) {
        self.gamma_w.scale_grad(s);
        self.gamma_b.scale_grad(s);
        self.beta_w.scale_grad(s);
        self.beta_b.scale_grad(s);
    }

    fn step(&mut self, hp: OptimHyper) {
        self.gamma_w.step_muon(hp);
        self.gamma_b.step_adamw(hp);
        self.beta_w.step_muon(hp);
        self.beta_b.step_adamw(hp);
    }

    fn forward(&self, x: &Array2<f32>, cond_vec: &Array1<f32>) -> (Array2<f32>, AdaLnCache) {
        assert_eq!(cond_vec.len(), self.cond_dim);

        let t = x.nrows();
        let c = x.ncols();
        let cf = c as f32;

        // 修正（前回提案5）: xhat に直接書き込み、y は xhat を再利用して上書きする。
        // 旧: xhat (t×c) + y (t×c) の2アロケーション → 新: xhat のみ（y は in-place で xhat から変換）
        let mut xhat = Array2::<f32>::zeros((t, c));
        let mut inv_std = Array1::<f32>::zeros(t);

        for i in 0..t {
            let row = x.slice(s![i, ..]);
            let mu = row.iter().sum::<f32>() / cf;
            let var = row.iter().map(|&v| { let d = v - mu; d * d }).sum::<f32>() / cf;
            let istd = 1.0 / (var + self.eps).sqrt();
            inv_std[i] = istd;
            let mut xhat_row = xhat.slice_mut(s![i, ..]);
            azip!((xh in xhat_row.view_mut(), &xi in row) *xh = (xi - mu) * istd);
        }

        let gamma = self.gamma_w.w.dot(cond_vec) + &self.gamma_b.w;
        let beta  = self.beta_w.w.dot(cond_vec)  + &self.beta_b.w;

        // y を別途確保せず xhat を直接 y に変換する（in-place、アロケーション節約）
        let mut y = xhat.clone(); // backward で xhat が必要なので clone は必要
        for i in 0..t {
            let mut y_row = y.slice_mut(s![i, ..]);
            azip!((yi in y_row.view_mut(), &gj in &gamma, &bj in &beta)
                *yi = *yi * (1.0 + gj) + bj
            );
        }

        (
            y,
            AdaLnCache {
                xhat,
                inv_std,
                gamma,
                cond_vec: cond_vec.clone(),
            },
        )
    }

    fn backward(&mut self, cache: &AdaLnCache, dy: &Array2<f32>) -> (Array2<f32>, Array1<f32>) {
        let t = dy.nrows();
        let c = dy.ncols();
        let cf = c as f32;

        let mut dgamma = Array1::<f32>::zeros(c);
        let mut dbeta  = Array1::<f32>::zeros(c);
        // 修正（前回提案5）: dxhat を dx に直接書き込み、後で in-place で LN の逆伝播を適用する。
        // 旧: dxhat (t×c) + dx (t×c) の2アロケーション → 新: dxhat のみ（dx として再利用）
        let mut dxhat  = Array2::<f32>::zeros((t, c));

        for i in 0..t {
            let dy_row   = dy.slice(s![i, ..]);
            let xhat_row = cache.xhat.slice(s![i, ..]);
            let mut dxhat_row = dxhat.slice_mut(s![i, ..]);
            for (j, ((&dy_ij, &xh), &gj)) in dy_row.iter()
                .zip(xhat_row.iter())
                .zip(cache.gamma.iter())
                .enumerate()
            {
                dgamma[j]    += dy_ij * xh;
                dbeta[j]     += dy_ij;
                dxhat_row[j]  = dy_ij * (1.0 + gj);
            }
        }

        self.gamma_w.gw += &dgamma
            .view().insert_axis(Axis(1))
            .dot(&cache.cond_vec.view().insert_axis(Axis(0)));
        self.gamma_b.gw += &dgamma;

        self.beta_w.gw += &dbeta
            .view().insert_axis(Axis(1))
            .dot(&cache.cond_vec.view().insert_axis(Axis(0)));
        self.beta_b.gw += &dbeta;

        let dcond_vec = self.gamma_w.w.t().dot(&dgamma) + self.beta_w.w.t().dot(&dbeta);

        // 修正（前回提案5）: dx を別途確保せず dxhat を in-place で LN 逆伝播に変換する。
        // 各行の sum1/sum2 を計算し、dxhat_row を直接 dx_row として上書きする。
        for i in 0..t {
            let xhat_row  = cache.xhat.slice(s![i, ..]);
            let istd = cache.inv_std[i];
            // sum1, sum2 を求めるため一時的にスライスを読む（dxhat は直後に上書き）
            let (sum1, sum2) = {
                let dxhat_row = dxhat.slice(s![i, ..]);
                let s1 = dxhat_row.sum();
                let s2 = dxhat_row.iter().zip(xhat_row.iter()).map(|(&a, &b)| a * b).sum::<f32>();
                (s1, s2)
            };
            let inv_c = 1.0 / cf;
            let mut dx_row = dxhat.slice_mut(s![i, ..]);
            // in-place: dxhat_row → dx_row
            azip!((dxi in dx_row.view_mut(), &xh in xhat_row)
                *dxi = inv_c * istd * (cf * *dxi - sum1 - xh * sum2)
            );
        }

        (dxhat, dcond_vec) // dxhat がそのまま dx として返る
    }
}

// ============================================================
// MLP
// ============================================================

#[derive(Clone)]
struct Mlp2D {
    fc1: Linear,
    fc2: Linear,
}

#[derive(Clone)]
struct Mlp2DCache {
    z1: Array2<f32>,
    fc1_cache: LinearCache,
    fc2_cache: LinearCache,
}

impl Mlp2D {
    fn new(rng: &mut StdRng, in_dim: usize, hidden_dim: usize, scale: f32) -> Self {
        Self {
            fc1: Linear::new(rng, in_dim, hidden_dim, scale),
            fc2: Linear::new(rng, hidden_dim, in_dim, scale),
        }
    }

    fn zero_grad(&mut self) {
        self.fc1.zero_grad();
        self.fc2.zero_grad();
    }

    fn scale_grad(&mut self, s: f32) {
        self.fc1.scale_grad(s);
        self.fc2.scale_grad(s);
    }

    fn step(&mut self, hp: OptimHyper) {
        self.fc1.step(hp);
        self.fc2.step(hp);
    }

    fn forward(&self, x: &Array2<f32>) -> (Array2<f32>, Mlp2DCache) {
        let (z1, fc1_cache) = self.fc1.forward(x);
        let a1 = z1.mapv(gelu_scalar);
        let (y, fc2_cache) = self.fc2.forward(&a1);
        (
            y,
            Mlp2DCache {
                z1,
                fc1_cache,
                fc2_cache,
            },
        )
    }

    fn backward(&mut self, cache: &Mlp2DCache, dy: &Array2<f32>) -> Array2<f32> {
        let da1 = self.fc2.backward(&cache.fc2_cache, dy);
        let mut dz1 = da1.clone();
        // 修正B: indexed_iter_mut + 2次元インデックス [[i,j]] を
        // フラットイテレータに置換 → 境界チェック排除 + SIMD autovectorization 促進
        for (v, &z) in dz1.iter_mut().zip(cache.z1.iter()) {
            *v *= gelu_derivative_scalar(z);
        }
        self.fc1.backward(&cache.fc1_cache, &dz1)
    }
}

// ============================================================
// Mixer block
// ============================================================

#[derive(Clone)]
struct MixerBlock {
    cond_proj: CondProjector,
    // Attention は完全削除。Mixer 本来の token-mixing MLP + channel-mixing MLP のみ使う。
    ln_tok: AdaLn,
    tok_mlp: Mlp2D,
    ln_ch: AdaLn,
    ch_mlp: Mlp2D,
}

#[derive(Clone)]
struct MixerBlockCache {
    cond_proj_cache: CondProjectorCache,
    ln_tok_cache: AdaLnCache,
    tok_mlp_cache: Mlp2DCache,
    ln_ch_cache: AdaLnCache,
    ch_mlp_cache: Mlp2DCache,
}

impl MixerBlock {
    fn new(
        rng: &mut StdRng,
        tokens: usize,
        channels: usize,
        token_hidden: usize,
        channel_hidden: usize,
        cond_emb_dim: usize,
        cond_proj_hidden: usize,
        cond_vec_dim: usize,
        scale: f32,
    ) -> Self {
        Self {
            cond_proj: CondProjector::new(rng, cond_emb_dim, cond_proj_hidden, cond_vec_dim, scale),
            ln_tok: AdaLn::new(channels, cond_vec_dim),
            tok_mlp: Mlp2D::new(rng, tokens, token_hidden, scale),
            ln_ch: AdaLn::new(channels, cond_vec_dim),
            ch_mlp: Mlp2D::new(rng, channels, channel_hidden, scale),
        }
    }

    fn zero_grad(&mut self) {
        self.cond_proj.zero_grad();
        self.ln_tok.zero_grad();
        self.tok_mlp.zero_grad();
        self.ln_ch.zero_grad();
        self.ch_mlp.zero_grad();
    }

    fn scale_grad(&mut self, s: f32) {
        self.cond_proj.scale_grad(s);
        self.ln_tok.scale_grad(s);
        self.tok_mlp.scale_grad(s);
        self.ln_ch.scale_grad(s);
        self.ch_mlp.scale_grad(s);
    }

    fn step(&mut self, hp: OptimHyper) {
        self.cond_proj.step(hp);
        self.ln_tok.step(hp);
        self.tok_mlp.step(hp);
        self.ln_ch.step(hp);
        self.ch_mlp.step(hp);
    }

    fn forward(&self, x: &Array2<f32>, cond_emb: &Array1<f32>) -> (Array2<f32>, MixerBlockCache) {
        let (cond_vec, cond_proj_cache) = self.cond_proj.forward(cond_emb);

        // Attention 経路は削除。O(T^2*C) の self-attention を走らせず、
        // token MLP は transpose した [channels, tokens] に対してだけ適用する。
        let (tok_norm, ln_tok_cache) = self.ln_tok.forward(x, &cond_vec);
        let tok_in = tok_norm.t().to_owned();
        let (tok_out_t, tok_mlp_cache) = self.tok_mlp.forward(&tok_in);
        let tok_out = tok_out_t.t().to_owned();
        let x1 = x + &tok_out;

        let (ch_norm, ln_ch_cache) = self.ln_ch.forward(&x1, &cond_vec);
        let (ch_out, ch_mlp_cache) = self.ch_mlp.forward(&ch_norm);
        let y = &x1 + &ch_out;

        (
            y,
            MixerBlockCache {
                cond_proj_cache,
                ln_tok_cache,
                tok_mlp_cache,
                ln_ch_cache,
                ch_mlp_cache,
            },
        )
    }

    fn backward(
        &mut self,
        cache: &MixerBlockCache,
        dy: &Array2<f32>,
    ) -> (Array2<f32>, Array1<f32>) {
        let cond_vec_dim = cache.ln_tok_cache.cond_vec.len();
        let mut dcond_vec = Array1::<f32>::zeros(cond_vec_dim);

        // 修正（前回提案7）: dy.clone() を2回していたのを1回に削減。
        // ch_mlp の backward は dy を view で受け取れるので、
        // dx1 への残差加算のために dy の clone を1回に統合する。
        let dch_norm = self.ch_mlp.backward(&cache.ch_mlp_cache, dy);
        let (dx1_from_ch, dc2) = self.ln_ch.backward(&cache.ln_ch_cache, &dch_norm);
        dcond_vec += &dc2;
        // dx1 = dy + dx1_from_ch （dy の clone は ここで1回だけ）
        let mut dx1 = dy + &dx1_from_ch;

        let dtok_out_t = dx1.t().to_owned();
        let dtok_in = self.tok_mlp.backward(&cache.tok_mlp_cache, &dtok_out_t);
        let dtok_norm = dtok_in.t().to_owned();
        let (dx0_from_tok, dc1) = self.ln_tok.backward(&cache.ln_tok_cache, &dtok_norm);
        dcond_vec += &dc1;
        dx1 += &dx0_from_tok;  // dx1 を dx0 として再利用（clone 不要）

        let dcond_emb = self.cond_proj.backward(&cache.cond_proj_cache, &dcond_vec);
        (dx1, dcond_emb)
    }

}


// ============================================================
// sample structs
// ============================================================

#[derive(Clone)]
struct Sample {
    a_mats: Vec<Array2<f32>>, // each is [A, 2*(A+N_INPUTS_ADF)]
    b_mat: Array2<f32>,       // [B, 2*(B+N_INPUTS_MAIN)]
    cond: Array1<f32>,
}

#[derive(Clone)]
struct Targets {
    a_targets: Vec<Array2<f32>>, // each is [A, 2*(A+N_INPUTS_ADF)]
    b_target: Array2<f32>,       // [B, 2*(B+N_INPUTS_MAIN)]
}

struct ModelOutput {
    a_logits: Vec<Array2<f32>>,
    b_logits: Array2<f32>,
}

struct ForwardCache {
    enc_a_caches: Vec<BilinearCache>,
    enc_b_cache: BilinearCache,
    cond_cache: CondEmbeddingCache,
    block_caches: Vec<MixerBlockCache>,
    dec_a_caches: Vec<BilinearCache>,
    dec_b_cache: BilinearCache,
}

// ============================================================
// full model
// ============================================================

struct MixerModel {
    num_a: usize,
    a_rows: usize,
    a_cols: usize,
    b_rows: usize,
    b_cols: usize,
    d1: usize,
    d2: usize,

    enc_a: BilinearMap,
    dec_a: BilinearMap,
    enc_b: BilinearMap,
    dec_b: BilinearMap,

    cond_embed: CondEmbedding,
    blocks: Vec<MixerBlock>,
}

impl MixerModel {
    #[allow(clippy::too_many_arguments)]
    fn new(
        rng: &mut StdRng,
        num_a: usize,
        a_rows: usize,
        a_cols: usize,
        b_rows: usize,
        b_cols: usize,
        d1: usize,
        d2: usize,
        num_blocks: usize,
        token_hidden: usize,
        channel_hidden: usize,
        cond_embed: CondEmbedding,
        cond_proj_hidden: usize,
        cond_vec_dim: usize,
    ) -> Self {
        let scale_a = (2.0 / (a_rows + d1 + a_cols + d2) as f32).sqrt();
        let scale_b = (2.0 / (b_rows + d1 + b_cols + d2) as f32).sqrt();
        let scale_inner = 0.05;

        let enc_a = BilinearMap::new(rng, a_rows, a_cols, d1, d2, scale_a);
        let dec_a = BilinearMap::new(rng, d1, d2, a_rows, a_cols, scale_a);
        let enc_b = BilinearMap::new(rng, b_rows, b_cols, d1, d2, scale_b);
        let dec_b = BilinearMap::new(rng, d1, d2, b_rows, b_cols, scale_b);

        let tokens = (num_a + 1) * d1;
        let cond_emb_dim = cond_embed.output_dim();

        let mut blocks = Vec::new();
        for _ in 0..num_blocks {
            blocks.push(MixerBlock::new(
                rng,
                tokens,
                d2,
                token_hidden,
                channel_hidden,
                cond_emb_dim,
                cond_proj_hidden,
                cond_vec_dim,
                scale_inner,
            ));
        }

        Self {
            num_a,
            a_rows,
            a_cols,
            b_rows,
            b_cols,
            d1,
            d2,
            enc_a,
            dec_a,
            enc_b,
            dec_b,
            cond_embed,
            blocks,
        }
    }

    fn zero_grad(&mut self) {
        self.enc_a.zero_grad();
        self.dec_a.zero_grad();
        self.enc_b.zero_grad();
        self.dec_b.zero_grad();
        for b in &mut self.blocks {
            b.zero_grad();
        }
    }

    fn scale_gradients(&mut self, s: f32) {
        self.enc_a.scale_grad(s);
        self.dec_a.scale_grad(s);
        self.enc_b.scale_grad(s);
        self.dec_b.scale_grad(s);
        for b in &mut self.blocks {
            b.scale_grad(s);
        }
    }

    fn step(&mut self, hp: OptimHyper) {
        self.enc_a.step(hp);
        self.dec_a.step(hp);
        self.enc_b.step(hp);
        self.dec_b.step(hp);
        for b in &mut self.blocks {
            b.step(hp);
        }
    }

    fn forward(&self, sample: &Sample) -> (ModelOutput, ForwardCache) {
        assert_eq!(sample.a_mats.len(), self.num_a);

        let mut parts: Vec<Array2<f32>> = Vec::new();
        let mut enc_a_caches = Vec::new();

        for x in &sample.a_mats {
            let (blk, cache) = self.enc_a.forward(x);
            parts.push(blk);
            enc_a_caches.push(cache);
        }

        let (b_blk, enc_b_cache) = self.enc_b.forward(&sample.b_mat);
        parts.push(b_blk);

        let total_tokens = (self.num_a + 1) * self.d1;
        let mut z = Array2::<f32>::zeros((total_tokens, self.d2));
        for (i, part) in parts.iter().enumerate() {
            let start = i * self.d1;
            let end = start + self.d1;
            z.slice_mut(s![start..end, ..]).assign(part);
        }

        let (cond_emb, cond_cache) = self.cond_embed.forward(&sample.cond);

        let mut block_caches = Vec::new();
        for block in &self.blocks {
            let (next, cache) = block.forward(&z, &cond_emb);
            z = next;
            block_caches.push(cache);
        }

        let mut a_logits = Vec::new();
        let mut dec_a_caches = Vec::new();

        for i in 0..self.num_a {
            let start = i * self.d1;
            let end = start + self.d1;
            let blk = z.slice(s![start..end, ..]).to_owned();
            let (mat, cache) = self.dec_a.forward(&blk);
            a_logits.push(mat);
            dec_a_caches.push(cache);
        }

        let start = self.num_a * self.d1;
        let end = start + self.d1;
        let blk_b = z.slice(s![start..end, ..]).to_owned();
        let (b_logits, dec_b_cache) = self.dec_b.forward(&blk_b);

        (
            ModelOutput { a_logits, b_logits },
            ForwardCache {
                enc_a_caches,
                enc_b_cache,
                cond_cache,
                block_caches,
                dec_a_caches,
                dec_b_cache,
            },
        )
    }

    fn bce_with_logits_loss(logits: &Array2<f32>, targets: &Array2<f32>) -> (f32, Array2<f32>) {
        let n = logits.len() as f32;
        let mut loss = 0.0f32;
        let mut grad = Array2::<f32>::zeros(logits.raw_dim());

        // indexed_iter ではなくフラットイテレーションで境界チェックを排除し SIMD を促進
        for ((&z, &t), g) in logits.iter().zip(targets.iter()).zip(grad.iter_mut()) {
            loss += z.max(0.0) - z * t + (1.0 + (-z.abs()).exp()).ln();
            *g = (sigmoid_scalar(z) - t) / n;
        }

        (loss / n, grad)
    }

    fn loss_and_backward_accumulate(
        &mut self,
        sample: &Sample,
        target: &Targets,
    ) -> (f32, ModelOutput) {
        let (output, cache) = self.forward(sample);

        let mut total_loss = 0.0;
        let mut dz = Array2::<f32>::zeros(((self.num_a + 1) * self.d1, self.d2));

        for i in 0..self.num_a {
            let (loss_i, dlogits) =
                Self::bce_with_logits_loss(&output.a_logits[i], &target.a_targets[i]);
            total_loss += loss_i;

            let dblk = self.dec_a.backward(&cache.dec_a_caches[i], &dlogits);
            let start = i * self.d1;
            let end = start + self.d1;
            let mut sl = dz.slice_mut(s![start..end, ..]);
            sl += &dblk;
        }

        let (loss_b, dlogits_b) = Self::bce_with_logits_loss(&output.b_logits, &target.b_target);
        total_loss += loss_b;

        let dblk_b = self.dec_b.backward(&cache.dec_b_cache, &dlogits_b);
        let start_b = self.num_a * self.d1;
        let end_b = start_b + self.d1;
        {
            let mut sl = dz.slice_mut(s![start_b..end_b, ..]);
            sl += &dblk_b;
        }

        let mut dcond_emb = Array1::<f32>::zeros(self.cond_embed.output_dim());

        for i in (0..self.blocks.len()).rev() {
            let (next_dz, dc) = self.blocks[i].backward(&cache.block_caches[i], &dz);
            dz = next_dz;
            dcond_emb += &dc;
        }

        let _dcond_vec = self.cond_embed.backward(&cache.cond_cache, &dcond_emb);

        for i in 0..self.num_a {
            let start = i * self.d1;
            let end = start + self.d1;
            let dblk = dz.slice(s![start..end, ..]).to_owned();
            let _dx = self.enc_a.backward(&cache.enc_a_caches[i], &dblk);
        }

        let dblk_b2 = dz.slice(s![start_b..end_b, ..]).to_owned();
        let _dx_b = self.enc_b.backward(&cache.enc_b_cache, &dblk_b2);

        let denom = (self.num_a + 1) as f32;
        (total_loss / denom, output)
    }

    fn train_minibatch_step(&mut self, batch: &[(Sample, Targets)], hp: OptimHyper) -> f32 {
        self.zero_grad();

        let mut loss_sum = 0.0;
        for (sample, target) in batch {
            let (loss, _) = self.loss_and_backward_accumulate(sample, target);
            loss_sum += loss;
        }

        let inv_bs = 1.0 / batch.len() as f32;
        self.scale_gradients(inv_bs);
        self.step(hp);

        loss_sum * inv_bs
    }

    fn predict(&self, sample: &Sample) -> ModelOutput {
        self.forward(sample).0
    }

    /// 複数サンプルを並列に推論する。各サンプルは独立なので rayon で並列化できる。
    /// 戻り値は入力と同順の `ModelOutput` のベクタ。
    fn predict_batch(&self, samples: &[Sample]) -> Vec<ModelOutput> {
        samples.par_iter().map(|s| self.forward(s).0).collect()
    }

    fn predict_probs(&self, sample: &Sample) -> (Vec<Array2<f32>>, Array2<f32>) {
        let out = self.predict(sample);
        let as_ = out
            .a_logits
            .into_iter()
            .map(|x| x.mapv(sigmoid_scalar))
            .collect::<Vec<_>>();
        let b = out.b_logits.mapv(sigmoid_scalar);
        (as_, b)
    }
}

// ============================================================
// hybrid controller glue
// ============================================================

#[derive(Debug, Clone)]
pub struct NearestBetterResult {
    /// ans[i] = score が自分より高く、かつハミング距離最小の個体 index
    /// 候補が無ければ None
    pub ans: Vec<Option<usize>>,
}

#[inline]
fn freq_bucket(f: usize) -> u8 {
    if f == 0 {
        0
    } else {
        (usize::BITS - f.leading_zeros()) as u8
    }
}

/// nearest-better-hamming 高速版 v2。
///
/// 改善点 (v1 からの追加):
///   5. active を swap-remove で即時縮小。
///      トゥームストーン方式では pruning 走査が O(|active_total|) だったが、
///      swap-remove により O(|alive|) に削減。alive が少ないほど効果大。
///   6. signature バッファをループ外で確保しクエリ間で再利用。
///      毎クエリの Vec アロケーション (O(l) malloc) を排除。
///   7. freqs を u32 に縮小。freq_bucket の入力はほぼ u32 範囲に収まる。
///   8. rem の減少でも pruning を発火させる「デュアルトリガー」を導入。
///      best_match が変わらなくても rem が閾値以上下がった場合に追加 pruning。
///      ただしトリガー間隔を対数的にスケジュールし O(l × |alive|) への退化を防ぐ。
///
/// arrs[k][i] は 0..alphabet_sizes[i]-1 にある必要がある。
pub fn nearest_better_hamming_pruned_generalized(
    arrs: &[Vec<usize>],
    scores: &[f64],
    alphabet_sizes: &[usize],
    max_cache_size: usize,
) -> NearestBetterResult {
    // 修正D: std::collections::HashMap (SipHash) を AHashMap に置換。
    // ahash は既に依存関係に含まれており、キャッシュ系操作が大幅に高速化される。
    use ahash::AHashMap;
    use std::rc::Rc;

    let n = arrs.len();
    assert_eq!(n, scores.len(), "arrs.len() must equal scores.len()");
    if n == 0 {
        return NearestBetterResult { ans: vec![] };
    }

    let l = arrs[0].len();
    assert_eq!(l, alphabet_sizes.len(), "alphabet_sizes.len() must equal code length");
    #[cfg(debug_assertions)]
    for a in arrs.iter() { assert_eq!(a.len(), l); }
    #[cfg(debug_assertions)]
    for x in arrs.iter() {
        for (i, &v) in x.iter().enumerate() {
            assert!(v < alphabet_sizes[i],
                "arr value out of range: x[{}]={}, expected 0..{}", i, v, alphabet_sizes[i]);
        }
    }

    let mut order: Vec<usize> = (0..n).collect();
    order.sort_by(|&i, &j| scores[j].partial_cmp(&scores[i]).unwrap_or(std::cmp::Ordering::Equal));

    let mut ans: Vec<Option<usize>> = vec![None; n];

    // postings / freqs を u32 に縮小 (n < 2^32 は実用上保証)
    let mut postings: Vec<Vec<Vec<u32>>> = alphabet_sizes
        .iter()
        .map(|&k| vec![Vec::<u32>::new(); k])
        .collect();
    let mut freqs: Vec<Vec<u32>> = alphabet_sizes
        .iter()
        .map(|&k| vec![0u32; k])
        .collect();

    let mut count: Vec<u32> = vec![0u32; n];
    let mut seen_ver: Vec<u32> = vec![0u32; n];
    let mut query_ver: u32 = 0;

    let mut order_cache: AHashMap<Vec<u8>, Rc<[usize]>> = AHashMap::new();

    // swap-remove 用 active。alive のみを保持し、pruning で即時縮小する。
    let mut active: Vec<u32> = Vec::with_capacity(512.min(n));

    // signature バッファをループ外で確保し、毎クエリ再利用 (malloc 排除)
    let mut signature: Vec<u8> = vec![0u8; l];

    let l_u32 = l as u32;

    let mut p = 0;
    while p < n {
        let s = scores[order[p]];
        let mut q = p + 1;
        while q < n && scores[order[q]] == s { q += 1; }

        let group = &order[p..q];

        for &idx in group.iter() {
            let x = &arrs[idx];

            // 座標順序の計算（周波数バケットシグネチャでキャッシュ）
            let coords: Rc<[usize]> = {
                // signature バッファを上書き再利用 (アロケーションなし)
                for i in 0..l { signature[i] = freq_bucket(freqs[i][x[i]] as usize); }
                if let Some(cached) = order_cache.get(&signature) {
                    Rc::clone(cached)
                } else {
                    let mut coords_vec: Vec<usize> = (0..l).collect();
                    coords_vec.sort_by_key(|&i| (signature[i], freqs[i][x[i]], i));
                    if order_cache.len() >= max_cache_size { order_cache.clear(); }
                    let rc: Rc<[usize]> = coords_vec.into();
                    order_cache.insert(signature.clone(), Rc::clone(&rc));
                    rc
                }
            };

            // クエリ本体
            query_ver += 1;
            let ver = query_ver;
            let mut best_id: Option<usize> = None;
            let mut best_match: u32 = 0;
            // pruning トリガー管理:
            //   last_pruned_best  : 前回発火時の best_match
            //   next_pruning_step : rem ベースの次回発火ステップ (対数スケジュール)
            let mut last_pruned_best: u32 = 0;
            let mut next_pruning_step: usize = l / 2; // 最初の rem トリガーは l/2 ステップ後
            active.clear();

            'query: for (step, &i) in coords.iter().enumerate() {
                let plist = &postings[i][x[i]];

                for &jraw in plist.iter() {
                    let j = jraw as usize;
                    if seen_ver[j] != ver {
                        seen_ver[j] = ver;
                        count[j] = 0;
                        active.push(jraw);   // alive のみ push
                    }
                    count[j] += 1;
                    if count[j] > best_match {
                        best_match = count[j];
                        best_id = Some(j);
                    }
                }

                if best_match == l_u32 { break; }

                // pruning 発火条件:
                //   A) best_match が上がった (必ず発火)
                //   B) 対数スケジュールのステップに到達 (rem の減少を段階的に反映)
                let do_prune = best_match > last_pruned_best || step >= next_pruning_step;
                if do_prune && best_match > 0 {
                    last_pruned_best = best_match;
                    // 次回 rem トリガーを現在の残り半分に設定 (対数分割)
                    next_pruning_step = step + (l - step).max(1) / 2;

                    let rem = (l - step - 1) as u32;
                    let threshold = best_match;
                    // swap-remove: alive のみを走査し、脱落個体を末尾と入れ替えて即時削除。
                    // retain と異なり O(|alive|) で、dead 要素を一切スキャンしない。
                    let mut wi = 0usize;
                    while wi < active.len() {
                        let j = active[wi] as usize;
                        if count[j] + rem < threshold {
                            // swap-remove: 末尾要素で上書きし len を縮める
                            active.swap_remove(wi);
                            // wi はインクリメントしない (新たに来た要素を次ループで判定)
                        } else {
                            wi += 1;
                        }
                    }
                    if best_id.is_some() && active.is_empty() {
                        break 'query;
                    }
                }
            }

            ans[idx] = best_id;
        }

        for &idx in group.iter() {
            let x = &arrs[idx];
            for i in 0..l {
                let v = x[i];
                postings[i][v].push(idx as u32);
                freqs[i][v] += 1;
            }
        }

        p = q;
    }

    NearestBetterResult { ans }
}

#[derive(Clone, Debug)]
pub struct HybridMutationConfig {
    pub learned_mutation_prob: f64,
    pub train_every_generations: usize,
    pub train_epochs_per_generation: usize,
    pub batch_size: usize,
    pub nearest_better_cache_size: usize,
    pub train_population_subset: usize,
    pub max_teacher_pairs: usize,
    pub max_minibatches_per_generation: usize,
}

impl Default for HybridMutationConfig {
    fn default() -> Self {
        Self {
            learned_mutation_prob: 0.15,
            train_every_generations: 1,
            train_epochs_per_generation: 1,
            batch_size: 8,
            nearest_better_cache_size: 1 << 14,
            train_population_subset: 256,
            max_teacher_pairs: 256,
            max_minibatches_per_generation: 16,
        }
    }
}

#[derive(Clone)]
struct ChromosomeRef {
    layer_idx: usize,
    chrom_idx: usize,
}

/// learned mutator では connection topology のみを学習し、
/// func スロットは classical mutation に残す。
#[derive(Clone)]
pub struct GenomeCodec {
    adf_order: Vec<ChromosomeRef>,
    adf_len: usize,
    top_len: usize,
    adf_domain_cap: usize,
    top_domain_cap: usize,
    alphabet_sizes: Vec<usize>,
}

impl GenomeCodec {
    pub fn new() -> Self {
        let mut adf_order = Vec::new();
        let mut alphabet_sizes = Vec::new();

        let top_layer = N_LAYERS - 1;
        let top_len = layer_len(top_layer);
        let top_domain_cap = N_INPUTS_MAIN + top_len;

        let adf_len = if N_LAYERS >= 2 { layer_len(0) } else { 0 };
        let adf_domain_cap = if N_LAYERS >= 2 { N_INPUTS_ADF + adf_len } else { 0 };

        for li in 0..top_layer {
            let count = layer_n_adf(li);
            let len = layer_len(li);
            let n_ext = layer_n_ext(li);
            assert_eq!(len, adf_len, "all non-top chromosomes must share the same length");
            for ci in 0..count {
                adf_order.push(ChromosomeRef { layer_idx: li, chrom_idx: ci });
                for i in 0..len {
                    alphabet_sizes.push(n_ext + i);
                    alphabet_sizes.push(n_ext + i);
                }
            }
        }

        for i in 0..top_len {
            alphabet_sizes.push(N_INPUTS_MAIN + i);
            alphabet_sizes.push(N_INPUTS_MAIN + i);
        }

        Self {
            adf_order,
            adf_len,
            top_len,
            adf_domain_cap,
            top_domain_cap,
            alphabet_sizes,
        }
    }

    pub fn num_adf_mats(&self) -> usize {
        self.adf_order.len()
    }

    pub fn adf_shape(&self) -> (usize, usize) {
        (self.adf_len, 2 * self.adf_domain_cap)
    }

    pub fn top_shape(&self) -> (usize, usize) {
        (self.top_len, 2 * self.top_domain_cap)
    }

    pub fn alphabet_sizes(&self) -> &[usize] {
        &self.alphabet_sizes
    }

    pub fn flatten_discrete(&self, g: &Genome) -> Vec<usize> {
        let mut out = Vec::with_capacity(self.alphabet_sizes.len());
        for r in &self.adf_order {
            let c = &g.layers[r.layer_idx][r.chrom_idx];
            let len = layer_len(r.layer_idx);
            for i in 0..len {
                out.push(c.conn[i][0] as usize);
                out.push(c.conn[i][1] as usize);
            }
        }

        let top = &g.layers[N_LAYERS - 1][0];
        for i in 0..self.top_len {
            out.push(top.conn[i][0] as usize);
            out.push(top.conn[i][1] as usize);
        }
        out
    }

    fn encode_chromosome(c: &Chromosome, len: usize, domain_cap: usize) -> Array2<f32> {
        let mut mat = Array2::<f32>::zeros((len, 2 * domain_cap));
        for i in 0..len {
            let c0 = c.conn[i][0] as usize;
            let c1 = c.conn[i][1] as usize;
            debug_assert!(c0 < domain_cap);
            debug_assert!(c1 < domain_cap);
            mat[[i, c0]] = 1.0;
            mat[[i, domain_cap + c1]] = 1.0;
        }
        mat
    }

    pub fn encode_sample(&self, g: &Genome, loss_cond: f32) -> Sample {
        let mut a_mats = Vec::with_capacity(self.adf_order.len());
        for r in &self.adf_order {
            let c = &g.layers[r.layer_idx][r.chrom_idx];
            a_mats.push(Self::encode_chromosome(c, self.adf_len, self.adf_domain_cap));
        }
        let top = &g.layers[N_LAYERS - 1][0];
        let b_mat = Self::encode_chromosome(top, self.top_len, self.top_domain_cap);
        Sample { a_mats, b_mat, cond: Array1::from_vec(vec![loss_cond]) }
    }

    pub fn encode_target(&self, g: &Genome) -> Targets {
        // encode_sample を経由せず直接構築。cond フィールドを生成して即捨てるコストを排除。
        let mut a_targets = Vec::with_capacity(self.adf_order.len());
        for r in &self.adf_order {
            let c = &g.layers[r.layer_idx][r.chrom_idx];
            a_targets.push(Self::encode_chromosome(c, self.adf_len, self.adf_domain_cap));
        }
        let top = &g.layers[N_LAYERS - 1][0];
        let b_target = Self::encode_chromosome(top, self.top_len, self.top_domain_cap);
        Targets { a_targets, b_target }
    }

    fn argmax_range(row: ndarray::ArrayView1<'_, f32>, start: usize, len: usize) -> usize {
        let mut best_idx = start;
        let mut best_val = f32::NEG_INFINITY;
        for j in start..(start + len) {
            let v = row[j];
            if v > best_val {
                best_val = v;
                best_idx = j;
            }
        }
        best_idx - start
    }

    fn decode_chromosome_into(
        c: &mut Chromosome,
        logits: &Array2<f32>,
        len: usize,
        domain_cap: usize,
        n_ext: usize,
    ) {
        for i in 0..len {
            let valid_conn = n_ext + i;
            let row = logits.index_axis(Axis(0), i);
            c.conn[i][0] = Self::argmax_range(row.view(), 0, valid_conn) as u16;
            c.conn[i][1] = Self::argmax_range(row.view(), domain_cap, valid_conn) as u16;
        }
    }

    pub fn decode_output(&self, template: &Genome, out: &ModelOutput) -> Genome {
        let mut g = template.clone();

        for (mat_idx, r) in self.adf_order.iter().enumerate() {
            let li = r.layer_idx;
            let c = &mut g.layers[li][r.chrom_idx];
            Self::decode_chromosome_into(
                c,
                &out.a_logits[mat_idx],
                self.adf_len,
                self.adf_domain_cap,
                layer_n_ext(li),
            );
        }

        let top = &mut g.layers[N_LAYERS - 1][0];
        Self::decode_chromosome_into(
            top,
            &out.b_logits,
            self.top_len,
            self.top_domain_cap,
            N_INPUTS_MAIN,
        );
        g
    }
}

pub struct HybridMutationController {
    pub codec: GenomeCodec,
    pub model: MixerModel,
    pub optim: OptimHyper,
    pub cfg: HybridMutationConfig,
    trained_pairs_last: usize,
}

impl HybridMutationController {
    #[allow(clippy::too_many_arguments)]
    pub fn new(
        rng: &mut rand::rngs::StdRng,
        cfg: HybridMutationConfig,
        d1: usize,
        d2: usize,
        num_blocks: usize,
        token_hidden: usize,
        channel_hidden: usize,
        cond_embed: CondEmbedding,
        cond_proj_hidden: usize,
        cond_vec_dim: usize,
        optim: OptimHyper,
    ) -> Self {
        let codec = GenomeCodec::new();
        let (a_rows, a_cols) = codec.adf_shape();
        let (b_rows, b_cols) = codec.top_shape();
        let model = MixerModel::new(
            rng,
            codec.num_adf_mats(),
            a_rows,
            a_cols,
            b_rows,
            b_cols,
            d1,
            d2,
            num_blocks,
            token_hidden,
            channel_hidden,
            cond_embed,
            cond_proj_hidden,
            cond_vec_dim,
        );
        Self {
            codec,
            model,
            optim,
            cfg,
            trained_pairs_last: 0,
        }
    }

    pub fn trained_pairs_last(&self) -> usize {
        self.trained_pairs_last
    }

    fn loss_stats(losses: &[f64]) -> (f32, f32) {
        if losses.is_empty() {
            return (0.0, 1.0);
        }
        let mean = losses.iter().sum::<f64>() / losses.len() as f64;
        let var = losses
            .iter()
            .map(|&x| {
                let d = x - mean;
                d * d
            })
            .sum::<f64>()
            / losses.len() as f64;
        let std = var.sqrt().max(1e-8);
        (mean as f32, std as f32)
    }

    pub fn normalize_loss(loss: f64, mean: f32, std: f32) -> f32 {
        (((loss as f32) - mean) / std).tanh()
    }

    pub fn build_teacher_pairs(
        &self,
        genomes: &[&Genome],
        losses: &[f64],
        rng: &mut rand::rngs::StdRng,
    ) -> Vec<(Sample, Targets)> {
        if genomes.is_empty() {
            return Vec::new();
        }

        let subset_cap = self.cfg.train_population_subset.max(2);
        let idxs: Vec<usize> = if genomes.len() <= subset_cap {
            (0..genomes.len()).collect()
        } else {
            let elite_take = (subset_cap / 2).max(1).min(genomes.len());
            let mut idxs: Vec<usize> = (0..elite_take).collect();
            if subset_cap > elite_take {
                let mut tail: Vec<usize> = (elite_take..genomes.len()).collect();
                tail.shuffle(rng);
                tail.truncate(subset_cap - elite_take);
                idxs.extend(tail);
                idxs.sort_unstable();
            }
            idxs
        };

        // clone を排除: &Genome 参照スライスで全操作を行う。
        // sub_genomes: Vec<Genome> → sub_genomes: Vec<&Genome> に変更し、
        // encode_sample / encode_target / flatten_discrete はすべて &Genome を取るため
        // cloneコストが O(subset × genome_size) から 0 に削減される。
        let sub_genomes: Vec<&Genome> = idxs.iter().map(|&i| genomes[i]).collect();
        let sub_losses: Vec<f64> = idxs.iter().map(|&i| losses[i]).collect();
        // self.codec を先に借用しておき、以降のクロージャが &self 全体をキャプチャしないようにする。
        // flatten_discrete / encode_sample / encode_target は全て rayon 並列化可能。
        let codec = &self.codec;
        let codes: Vec<Vec<usize>> = sub_genomes
            .par_iter()
            .map(|g| codec.flatten_discrete(g))
            .collect();
        let scores: Vec<f64> = sub_losses.iter().map(|&x| -x).collect();

        let nb = nearest_better_hamming_pruned_generalized(
            &codes,
            &scores,
            codec.alphabet_sizes(),
            self.cfg.nearest_better_cache_size,
        );

        let (loss_mean, loss_std) = Self::loss_stats(&sub_losses);

        let best_loss = sub_losses
            .iter()
            .copied()
            .fold(f64::INFINITY, f64::min);

        // ペア収集後にまとめて encode_sample/encode_target を rayon 並列実行する。
        // 旧実装では直列ループ内で encode が走っていたが、各ペアは独立しているため
        // par_iter で並列化することで encode コストを O(pairs) → O(pairs/cores) に削減。
        let pair_indices: Vec<(usize, usize, f32)> = nb.ans.into_iter().enumerate()
            .filter_map(|(i, maybe_j)| {
                if (sub_losses[i] - best_loss).abs() <= 1e-15 { return None; }
                let j = maybe_j?;
                if j == i || codes[i] == codes[j] { return None; }
                let loss_cond = Self::normalize_loss(sub_losses[i], loss_mean, loss_std);
                Some((i, j, loss_cond))
            })
            .collect();

        let mut out: Vec<(Sample, Targets)> = pair_indices
            .par_iter()
            .map(|&(i, j, loss_cond)| {
                (
                    codec.encode_sample(sub_genomes[i], loss_cond),
                    codec.encode_target(sub_genomes[j]),
                )
            })
            .collect();
        if out.len() > self.cfg.max_teacher_pairs {
            out.shuffle(rng);
            out.truncate(self.cfg.max_teacher_pairs);
        }
        out
    }

    pub fn train_on_population(
        &mut self,
        genomes: &[&Genome],
        losses: &[f64],
        rng: &mut rand::rngs::StdRng,
    ) {

        // nearest_better + encode が重いため、開始・完了を個別にログする。
        // Python 側でサイレンスタイムアウトが誤発火しないよう定期的に stdout を出す。
        println_flush!("CMAES_STAGE stage=build_pairs_begin subset={}", genomes.len());
        let mut pairs = self.build_teacher_pairs(genomes, losses, rng);
        self.trained_pairs_last = pairs.len();
        println_flush!("CMAES_STAGE stage=build_pairs_end pairs={}", pairs.len());
        if pairs.is_empty() {
            return;
        }

        let mut steps = 0usize;
        let max_steps = self.cfg.max_minibatches_per_generation.max(1);
        for epoch in 0..self.cfg.train_epochs_per_generation {
            pairs.shuffle(rng);
            for batch in pairs.chunks(self.cfg.batch_size.max(1)) {
                self.model.train_minibatch_step(batch, self.optim);
                steps += 1;
                // steps==1 でも必ずログする（train_begin 後の最初の進捗を即時通知）
                if steps == 1 || steps % TRAIN_PROGRESS_EVERY == 0 || steps == max_steps {
                    println_flush!(
                        "CMAES_STAGE stage=train_progress epoch={} step={} max_steps={} pairs={}",
                        epoch,
                        steps,
                        max_steps,
                        pairs.len(),
                    );
                }
                if steps >= max_steps {
                    return;
                }
            }
        }
    }

    pub fn propose_from_model(&self, parent: &Genome, loss_cond: f32) -> Genome {
        let sample = self.codec.encode_sample(parent, loss_cond);
        let out = self.model.predict(&sample);
        self.codec.decode_output(parent, &out)
    }

    pub fn maybe_mutate_with_model(
        &self,
        rng: &mut rand::rngs::SmallRng,
        parent: &Genome,
        fallback: Genome,
        loss_cond: f32,
    ) -> Genome {
        if rng.gen::<f64>() < self.cfg.learned_mutation_prob {
            self.propose_from_model(parent, loss_cond)
        } else {
            fallback
        }
    }

    /// 次世代生成をバッチ化する。
    ///
    /// 重要: use_model == true の個体では classical child を先に作らない。
    /// 旧実装は mix/mutate した Genome を作ってからモデル出力で上書きしていたため、
    /// train_end → 次世代 eval_begin の間に無駄な Genome clone / mutate / dealloc が発生していた。
    pub fn apply_model_batch(
        &self,
        candidates: Vec<(Option<Genome>, bool, f32)>, // (classical_if_needed, use_model, loss_cond)
        parents: &[&Genome],
    ) -> Vec<Genome> {
        let model_indices: Vec<usize> = candidates
            .iter()
            .enumerate()
            .filter_map(|(i, (_, use_m, _))| if *use_m { Some(i) } else { None })
            .collect();

        if model_indices.is_empty() {
            return candidates.into_iter().map(|(g, _, _)| g.expect("classical child missing")).collect();
        }

        // encode_sample もかなり重いので並列化する。
        let samples: Vec<Sample> = model_indices
            .par_iter()
            .map(|&i| {
                let (_, _, loss_cond) = &candidates[i];
                self.codec.encode_sample(parents[i], *loss_cond)
            })
            .collect();
        println_flush!("CMAES_STAGE stage=offspring_model_encode_done model_children={}", samples.len());

        let outputs = self.model.predict_batch(&samples);
        println_flush!("CMAES_STAGE stage=offspring_model_predict_done model_children={}", outputs.len());

        // HashMap をやめ、index 対応の Vec<Option<Genome>> に直接格納する。
        let mut decoded: Vec<Option<Genome>> = (0..candidates.len()).map(|_| None).collect();
        let decoded_pairs: Vec<(usize, Genome)> = model_indices
            .par_iter()
            .zip(outputs.par_iter())
            .map(|(&cand_idx, out)| (cand_idx, self.codec.decode_output(parents[cand_idx], out)))
            .collect();
        for (idx, g) in decoded_pairs {
            decoded[idx] = Some(g);
        }
        println_flush!("CMAES_STAGE stage=offspring_model_decode_done model_children={}", model_indices.len());

        candidates
            .into_iter()
            .enumerate()
            .map(|(i, (classical, use_m, _))| {
                if use_m {
                    decoded[i].take().expect("model output missing")
                } else {
                    classical.expect("classical child missing")
                }
            })
            .collect()
    }
}

// ─── Main ─────────────────────────────────────────────────────────────────────

fn main() {
    assert_eq!(LAYER_LEN.len(), N_LAYERS, "LAYER_LEN の要素数は N_LAYERS に合わせてください");
    assert_eq!(N_ADF_PER_LAYER.len(), N_LAYERS - 1, "N_ADF_PER_LAYER の要素数は N_LAYERS-1 に合わせてください");
    assert!(N_LAYERS >= 1, "N_LAYERS は 1 以上にしてください");

    eprintln!(
        "ADF-CGP  N_LAYERS={N_LAYERS}  N_ADF_PER_LAYER={:?}  LAYER_LEN={:?}  POP={POP_SIZE}  BATCHES={N_BATCHES}  MUT_STOP_PROB={MUT_STOP_PROB}  MUT_MAX_TARGETS={MUT_MAX_TARGETS}  LEARNED_MUT_PROB={LEARNED_MUTATION_PROB}",
        N_ADF_PER_LAYER, LAYER_LEN,
    );

    let mut rng = SmallRng::seed_from_u64(42);
    let mut mlp_rng = StdRng::seed_from_u64(123456789);

    let ds = Dataset::new_fixed(&mut rng);
    let mut pop: Vec<Genome> = (0..POP_SIZE).map(|_| Genome::random(&mut rng)).collect();
    let evaluator = Evaluator::new();

    let hybrid_cfg = HybridMutationConfig {
        learned_mutation_prob: LEARNED_MUTATION_PROB,
        train_every_generations: MIXER_TRAIN_EVERY,
        train_epochs_per_generation: MIXER_TRAIN_EPOCHS,
        batch_size: MIXER_BATCH_SIZE,
        nearest_better_cache_size: NEAREST_BETTER_CACHE_SIZE,
        train_population_subset: MIXER_TRAIN_POP_SUBSET,
        max_teacher_pairs: MIXER_MAX_TEACHER_PAIRS,
        max_minibatches_per_generation: MIXER_MAX_MINIBATCHES,
    };

    let mixer_optim = OptimHyper {
        lr_matrix: MIXER_LR_MATRIX,
        muon_weight_decay: MUON_WEIGHT_DECAY,
        muon_momentum: MUON_MOMENTUM,
        muon_ns_steps: MUON_NS_STEPS,
        muon_nesterov: MUON_NESTEROV,
        lr_vector: MIXER_LR_VECTOR,
        adamw_beta1: ADAMW_BETA1,
        adamw_beta2: ADAMW_BETA2,
        adamw_eps: ADAMW_EPS,
        adamw_weight_decay: ADAMW_WEIGHT_DECAY,
    };

    let cond_embed = CondEmbedding::new_raw_vec(1);
    let mut learned_mutator = HybridMutationController::new(
        &mut mlp_rng,
        hybrid_cfg,
        MIXER_D1,
        MIXER_D2,
        MIXER_BLOCKS,
        MIXER_TOKEN_HIDDEN,
        MIXER_CHANNEL_HIDDEN,
        cond_embed,
        MIXER_COND_PROJ_HIDDEN,
        MIXER_COND_VEC_DIM,
        mixer_optim,
    );

    for gen in 0..N_GEN {
        println_flush!("CMAES_STAGE gen={} stage=eval_begin", gen);
        evaluator.reset_generation();
        let inter_weight = INTER_WEIGHT_MAX * (gen as f64 / CURRICULUM_RAMP_GENS as f64).min(1.0);

        let eval_chunk = EVAL_PROGRESS_CHUNK.max(1);
        let mut scored: Vec<(f64, f64, Genome)> = Vec::with_capacity(pop.len());
        for (chunk_idx, chunk) in pop.chunks(eval_chunk).enumerate() {
            let mut part: Vec<(f64, f64, Genome)> = chunk
                .par_iter()
                .map(|g| { let (l, a) = eval(g, &ds, &evaluator, inter_weight); (l, a, g.clone()) })
                .collect();
            scored.append(&mut part);
            println_flush!(
                "CMAES_STAGE gen={} stage=eval_progress chunk={} done={}/{}",
                gen,
                chunk_idx,
                scored.len(),
                pop.len(),
            );
        }
        // 完全ソートは不要。上位 ELITE 個が先頭に来れば十分。
        // select_nth_unstable で O(N) にピボット分割し、先頭 ELITE 個だけ sort する。
        // これにより全体 O(N log N) → O(N + ELITE log ELITE) に削減。
        if scored.len() > ELITE {
            scored.select_nth_unstable_by(ELITE, |a, b| {
                a.0.partial_cmp(&b.0).unwrap_or(std::cmp::Ordering::Equal)
            });
            scored[..ELITE].sort_unstable_by(|a, b| {
                a.0.partial_cmp(&b.0).unwrap_or(std::cmp::Ordering::Equal)
            });
        } else {
            scored.sort_unstable_by(|a, b| a.0.partial_cmp(&b.0).unwrap_or(std::cmp::Ordering::Equal));
        }

        // losses は scored への参照のみ。genomes_for_training の全クローンを排除。
        let losses: Vec<f64> = scored.iter().map(|x| x.0).collect();
        println_flush!(
            "CMAES_ACC gen={} loss={:.10} acc={:.10}",
            gen, scored[0].0, scored[0].1
        );

        // elites clone と loss_stats を train の前に前倒し計算。
        // ─ これにより train_end 直後の直列ブロックが消え、
        //   train_end → offspring_begin の間隔がほぼゼロになる。
        // ─ scored への共有不変参照なのでトレーニングと並走しても安全。
        let elites: Vec<Genome> = scored[..ELITE].iter().map(|x| x.2.clone()).collect();
        let (loss_mean, loss_std) = HybridMutationController::loss_stats(&losses);

        // specs（offspring 選択メタデータ）も train と並走して事前生成する。
        // スペックはシードだけ確定させ、実際の Genome 生成は後段の rayon で行う。
        let n_children_pre = POP_SIZE.saturating_sub(ELITE);
        let mut spec_seeds: Vec<(usize, usize, u8)> = (0..n_children_pre)
            .map(|_| {
                let i1 = rng.gen_range(0..ELITE);
                let i2 = rng.gen_range(0..ELITE);
                let op = rng.gen_range(0..3u8);
                (i1, i2, op)
            })
            .collect();

        let learned_mutator_active = gen >= MIXER_WARMUP_GENERATIONS;
        if learned_mutator_active && MIXER_TRAIN_EVERY > 0 && gen % MIXER_TRAIN_EVERY == 0 {
            println_flush!("CMAES_STAGE gen={} stage=train_begin", gen);
            // scored への参照スライスを渡す。クローンは train_on_population 内の
            // subset 選択時にのみ発生する（全個体クローンを排除）。
            let genomes_ref: Vec<&Genome> = scored.iter().map(|x| &x.2).collect();
            learned_mutator.train_on_population(
                &genomes_ref,
                &losses,
                &mut mlp_rng,
            );
            println_flush!(
                "CMAES_STAGE gen={} stage=train_end teacher_pairs={} subset={} max_pairs={} max_steps={}",
                gen,
                learned_mutator.trained_pairs_last(),
                MIXER_TRAIN_POP_SUBSET,
                MIXER_MAX_TEACHER_PAIRS,
                MIXER_MAX_MINIBATCHES,
            );
        } else {
            learned_mutator.trained_pairs_last = 0;
            if !learned_mutator_active {
                println_flush!(
                    "CMAES_STAGE gen={} stage=warmup mixer_warmup_generations={}",
                    gen,
                    MIXER_WARMUP_GENERATIONS,
                );
            }
        }

        // ── 次世代生成（バッチ化）────────────────────────────────────────────
        //
        // 旧実装: POP_SIZE 回のループ内で maybe_mutate_with_model を直列呼び出し
        //         → 1 個体ずつ MLP forward が走り O(POP_SIZE × forward_cost)
        //
        // 新実装:
        //   0. elites clone / loss_stats / spec_seeds (i1,i2,op) を train より前に前倒し
        //      → train_end 直後の直列ブロックをほぼゼロにする
        //   1. classical 変異を rayon で並列生成（CPU バウンド部分を並列化）
        //   2. use_model フラグを事前サンプリング（train 後、乱数消費のみ）
        //   3. use_model==true の個体のみサンプルをまとめて predict_batch に投げる
        //      → MLP forward が rayon 並列になり O(forward_cost) に近づく
        //   4. 結果をマージして next 世代を構築

        // n_children_pre / spec_seeds は train より前のブロックで確定済み。
        // use_model フラグと loss_cond だけここで付与する（乱数消費が最小限）。
        let n_children = n_children_pre;

        struct ChildSpec {
            i1: usize,
            i2: usize,
            op: u8,
            use_model: bool,
            loss_cond: f32,
        }
        // step1: use_model フラグと loss_cond を付与（parent/op シードは前倒し済み）
        let mut specs: Vec<ChildSpec> = spec_seeds
            .into_iter()
            .map(|(i1, i2, op)| {
                let use_model = learned_mutator_active
                    && rng.gen::<f64>() < learned_mutator.cfg.learned_mutation_prob;
                let loss_cond = HybridMutationController::normalize_loss(
                    scored[i1].0, loss_mean, loss_std,
                );
                ChildSpec { i1, i2, op, use_model, loss_cond }
            })
            .collect();

        // 推論子が POP_SIZE 比例で膨らむと、ここから offspring_end までで
        // encode_sample / MLP forward / decode_output が支配的になる。
        // 学習で使う上限より多いモデル子は費用対効果が悪いため、超過分は classical に戻す。
        let max_model_children = if learned_mutator_active {
            MIXER_MAX_TEACHER_PAIRS.min(MIXER_TRAIN_POP_SUBSET).min(n_children).max(1)
        } else { 0 };
        let mut kept_model_children = 0usize;
        let mut throttled_model_children = 0usize;
        for s in specs.iter_mut() {
            if s.use_model {
                if kept_model_children < max_model_children {
                    kept_model_children += 1;
                } else {
                    s.use_model = false;
                    throttled_model_children += 1;
                }
            }
        }

        println_flush!(
            "CMAES_STAGE gen={} stage=offspring_begin children={} model_active={} model_cap={} model_kept={} model_throttled={}",
            gen,
            n_children,
            learned_mutator_active,
            max_model_children,
            kept_model_children,
            throttled_model_children,
        );

        // step2: classical 変異を rayon で並列生成
        //        ただし use_model==true の子は後でモデル出力に置換されるので、ここでは作らない。
        //        旧実装では捨てる Genome を POP_SIZE * LEARNED_MUTATION_PROB 個ぶん生成していた。
        let candidates: Vec<(Option<Genome>, bool, f32)> = specs
            .par_iter()
            .map(|s| {
                if s.use_model {
                    return (None, true, s.loss_cond);
                }
                let seed = s.i1 as u64 ^ ((s.i2 as u64) << 16) ^ ((s.op as u64) << 32);
                let mut local_rng = SmallRng::seed_from_u64(seed);
                let p1 = &scored[s.i1].2;
                let p2 = &scored[s.i2].2;
                let child = match s.op {
                    0 => p1.mix(&mut local_rng, p2),
                    1 => p1.mutate(&mut local_rng),
                    _ => p1.mix(&mut local_rng, p2).mutate(&mut local_rng),
                };
                (Some(child), false, s.loss_cond)
            })
            .collect();

        let model_count = candidates.iter().filter(|(_, use_m, _)| *use_m).count();
        println_flush!(
            "CMAES_STAGE gen={} stage=offspring_classical_done model_children={} classical_children={}",
            gen,
            model_count,
            n_children.saturating_sub(model_count),
        );

        // step3: use_model==true の子孫にモデル推論をバッチ適用
        let parent_refs: Vec<&Genome> = specs.iter().map(|s| &scored[s.i1].2).collect();
        let model_children = if learned_mutator_active {
            learned_mutator.apply_model_batch(candidates, &parent_refs)
        } else {
            candidates.into_iter().map(|(g, _, _)| g.expect("classical child missing")).collect()
        };

        println_flush!("CMAES_STAGE gen={} stage=offspring_end", gen);

        let mut next = elites;
        next.extend(model_children);
        pop = next;

        // scored の Drop を別スレッドに委ねる。
        //
        // scored は Vec<(f64, f64, Genome)> で POP_SIZE 個の Genome を保持している。
        // Genome → Vec<Vec<Chromosome>> → Box<[u16;2]>/Box<[u8]> が再帰的に解放されるため、
        // 大きいパラメータ設定では dealloc だけで数百ms かかる。
        // これが train_end → 次世代 eval_begin の間に観測される遅延の原因。
        //
        // drop を別スレッドに投げることで、次世代の eval（rayon 並列）と重なり
        // レイテンシとして見えなくなる。
        std::thread::spawn(move || drop(scored));
    }
}


